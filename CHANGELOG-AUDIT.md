# Sapurai — Suivi de l'audit et des ameliorations

> Fichier de suivi pour faciliter la reprise apres une pause.
> Derniere mise a jour : 2026-05-17 (191 taches)

---

## FAIT — Sprint 45 : Tests rules emulateur 21/21 + observabilite dual-write

| # | Tache | Fichiers modifies | Details |
|---|-------|-------------------|---------|
| 187 | **Instrumentation - persistMirrorErrors + dedoublonnage 60s** | `src/services/dualwrite.ts`, `src/useData.ts`, `firestore.rules` | Sprint 44 avait laisse un trou : les erreurs du dual-write etaient logguees uniquement en `console.warn` (volatile). Sprint 45 ajoute `persistMirrorErrors(db, cid, stats)` qui parse chaque message `stats.errors[]` en `{entity, entityId, mirrorTarget, errorMessage, uid, sessionId, ts, durationMs}` et fait `addDoc()` dans `companies/{cid}/dual_write_errors/{auto_id}`. Fire-and-forget total. Cache de dedoublonnage 60s en memoire (`Map<key,ts>`, cleanup auto si >200 entrees) pour eviter de spammer la collection en cas d'erreur recurrente. Branche dans `useData.save()` apres `logMirrorResult()`. Rules : `read: admin only`, `create: tout member actif`, `update/delete: super-admin only` (immutabilite audit). |
| 188 | **Script diff mono-doc vs sous-collections** | `scripts/diff-mono-vs-sub.mjs` (CREE) | CLI Node Admin qui compare le mono-doc `/companies/{cid}` avec ses 5 sous-collections (dossiers, tcs, chs, dep, logs). Pour chaque entite : `onlyMono` (ids absents du miroir), `onlySub` (ids fantomes), `fieldDiffs` (diff field-by-field via `compareShallow`). Exit code 0 (OK) ou 2 (drift). Usage : `node scripts/diff-mono-vs-sub.mjs --cid=c_X [--entities=dos,tcs] [--verbose]`. A lancer 1x/jour pendant la fenetre d'observation Phase A (7 jours) - critere feu vert Phase C : 7 jours consecutifs avec zero drift. |
| 189 | **+10 tests rules Firestore (sous-collections + dual_write_errors)** | `src/__tests__/rules/firestore-security.test.ts` | Sprint 42 avait 11 tests rules pour la faille auto-adhesion. Sprint 45 ajoute 6 tests pour les sous-collections Sprint 43+44 : logs append-only (create OK, update/delete bloque), chs admin+editor only (agent refuse), dossiers agent peut update mais delete admin only, tcs tous roles create/update viewer refuse, dep agent create/update pas delete, dual_write_errors read admin only create member update/delete bloque. Total 17 tests Firestore. |
| 190 | **+4 tests rules Storage (isolation par membership)** | `src/__tests__/rules/storage-security.test.ts` (CREE) | Sprint 42 F42.3 avait remplace `allow read, write: if request.auth != null` par `match /files/{companyId}/{fileId=**}` qui exige membership Firestore. Sprint 45 ajoute 4 tests : refuse upload cross-tenant, refuse read cross-tenant, autorise upload propre company, refuse acces hors `/files/*`. Setup specifique avec `initializeTestEnvironment({ firestore: {...}, storage: {...} })` + 2 emulateurs sur 8080/9199. Total 21 tests rules. |
| 191 | **Hotfix critique hasValidInvite + branches mutex** | `firestore.rules` | Le test "refuse auto-join admin sans invitation" (faille P0.1 Sprint 40) echouait : la rule laissait passer un setDoc sans inviteCode. Cause : ma 1ere refonte avec `let inv = get(...).data;` ne compilait pas en emulateur (syntaxe peut-etre non supportee), donc les rules etaient ignorees silencieusement → comportement par defaut permissif. Hotfix : (1) refonte `hasValidInvite()` sans `let`, multiples `get()` inlines mais court-circuit garanti par `request.resource.data.keys().hasAll(['inviteCode'])` en premier ; (2) `allow create:` restructure en 2 branches mutuellement exclusives - BRANCHE 1 : 1er createur ET pas d'inviteCode dans le doc ; BRANCHE 2 : inviteCode present ET valide. Garde additionnelle `resource == null` pour vrai create. |
| | **Validation finale Sprint 45** | — | Build OK, typecheck 0 erreur, 380/380 tests app + **21/21 tests rules via emulateur Firebase** (Firestore + Storage). Couverture complete : auto-adhesion (P0.1 Sprint 40), sous-collections par role (Sprint 44 F44.3), dual_write_errors observabilite (Sprint 45), Storage cross-tenant (Sprint 42 F42.3). Cross-env installe pour Windows. **Effet metier** : (1) la faille P0.1 est maintenant TESTEE et validee par CI/CD potentielle ; (2) les erreurs silencieuses du dual-write sont enregistrees et observables par admin ; (3) regression-proof sur la matrice de permissions complete. **Prochaine etape** : execution backfill prod (Phase B) puis fenetre d'observation 7 jours avant Phase C. |

---

## FAIT — Sprint 44 : Durcissement permissions + nettoyage (suite audit reprise)

| # | Tache | Fichiers modifies | Details |
|---|-------|-------------------|---------|
| 182 | **F44.1 - cross-env installe** | `package.json` | `npm run test:rules` echouait sur Windows car la variable d'env `FIRESTORE_EMULATOR_HOST=...` etait passee en syntaxe Unix. Ajout `cross-env@^10.1.0` en devDep, script test:rules peut maintenant tourner sur tout OS (encore besoin d'un emulateur lance pour passer du SKIPPED a PASSED). |
| 183 | **F44.2 - src/firestore.rules legacy supprime** | `src/firestore.rules` (SUPPRIME) | Le fichier legacy `src/firestore.rules` (2.6 KB, daté Feb 22) coexistait avec `firestore.rules` (racine, le vrai) sans etre reference par firebase.json. Risque de confusion : un dev pouvait modifier le mauvais fichier. Supprime pour eliminer l'ambiguite. Seul `firestore.rules` racine reste authoritative. |
| 184 | **F44.3 - Rules sous-collections durcies par role** | `firestore.rules` | Sprint 43 avait pose des rules genericques `admin\|editor\|agent` pour les 5 nouvelles sous-collections. Sprint 44 introduit une matrice fine : `dossiers/*` create/update admin+editor+agent, delete admin uniquement (audit metier); `tcs/*` create/update tous les roles, delete admin+editor; `chs/*` create/update/delete admin+editor uniquement (agent ne gere pas les chauffeurs); `dep/*` create/update tous, delete admin+editor; `logs/*` APPEND-ONLY (create autorise pour tous, update/delete bloque sauf super-admin). |
| 185 | **F44.4 - Reduction warnings lint Sprint 43** | `src/services/dualwrite.ts` | Audit reprise a note +10 warnings depuis Sprint 43 (762 -> 772). 9 etaient des `any` dans dualwrite.ts. Re-type : `Record<string, unknown>` au lieu de `any` pour newData/prevData/items, `e instanceof Error ? e.message : 'unknown'` pour les catch. Eslint-disable explicite + raison pour le `console.warn` volontaire (debug Phase A). Resultat : 763 warnings (772 -> 763 = -9). |
| 186 | **F44.5 - Restrictions companies/* via affectedKeys** | `firestore.rules` | Helper `changesAdminOnlyFields()` qui detecte si l'update touche `cfg` ou `name` (tarifs societe + identite). Rule durcie : admin peut tout, editor/agent peuvent modifier les arrays operationnels (dos/tcs/chs/dep/logs) MAIS pas cfg ni name. Note transitoire : tant que save() reecrit tout le doc, affectedKeys() peut inclure cfg meme sans changement reel. Si bug en prod, retirer la condition. Effective totale en Phase D (Sprint 45+) apres migration sous-collections + passage updateDoc cible. |
| | **Verifications globales Sprint 44** | — | Build 29.75s, typecheck 0 erreur, 380/380 tests + 11 rules skipped, lint 763 warnings (-9 vs 772 audit reprise), 0 vulnerabilites npm. **Effet metier** : (1) agents et editors ne peuvent plus changer les tarifs (cfg) ni le nom de societe; (2) les chauffeurs et la suppression de dossiers sont reservees aux roles admin/editor; (3) les logs sont append-only (immutables une fois ecrits, ferme un vecteur de manipulation d'audit); (4) plus de confusion sur le fichier rules source de verite; (5) tests rules executables sur Windows. **Reste pour Sprint 45** : 4 issues encore ouvertes - migration listeners onSnapshot vers sous-collections (Phase C), cleanup arrays mono-doc (Phase D), reduction lint <500 warnings, refactor 60 tests E2E. |

---

## FAIT — Sprint 43 : Migration sous-collections Firestore - Phase A (dual-write) + B (script backfill)

| # | Tache | Fichiers modifies | Details |
|---|-------|-------------------|---------|
| 178 | **Phase A1 - dualwrite.ts** | `src/services/dualwrite.ts` (CREE) | Module isole qui implemente `mirrorToSubcollections(db, companyId, newData, prevData?)`. Pour chaque sous-collection (dossiers, tcs, chs, dep, logs) : batch write des items presents (set par id = idempotent) + delete des items absents si prevData fourni. Batch chunke a 400 (limite Firestore 500). Fire-and-forget : NE THROW JAMAIS, retourne `MirrorStats { ok, written, deleted, errors, durationMs }`. Helper `logMirrorResult` pour debug discret. |
| 179 | **Phase A2 - save() branche** | `src/useData.ts` | Apres le `setDoc(/companies/{cid})` (authoritative), on appelle `mirrorToSubcollections` avec le snapshot precedent en parametre. Fire-and-forget : un echec mirror ne bloque pas l'utilisateur ni le toast saveOk. prevData (l'ancien `data` React) permet de detecter les suppressions et de propager les deletes vers les sous-docs. |
| 180 | **Phase A3 - firestore.rules sous-collections** | `firestore.rules` | Ajout de 5 blocs `match /companies/{cid}/{dossiers,tcs,chs,dep,logs}/{id}` : `allow read: if isMember(cid)`, `allow write: if memberHasRole(['admin','editor','agent']) && isCompanyActive(cid)`. Alignement avec les rules du doc parent. En Phase A ces rules servent uniquement au miroir fire-and-forget. En Phase C (Sprint 44), elles deviendront authoritative quand on switchera les listeners. |
| 181 | **Phase B - scripts/backfill-subcollections.mjs** | `scripts/backfill-subcollections.mjs` (CREE) | Script Node Firebase Admin pour "exploser" les arrays imbriques des docs companies existants vers les sous-collections. Idempotent (setDoc ecrase). Options : `--apply` (sinon dry-run), `--only=companyId` (cibler une seule company). Batch chunke a 400. Logs detailles par company avec counts dos/tcs/chs/dep/logs. Requiert `GOOGLE_APPLICATION_CREDENTIALS` pointant vers un service account avec role Firestore Admin. **A executer une fois** quand Phase A aura tourne 7 jours sans erreur miroir (cf MIGRATION-SUBCOLLECTIONS.md). |
| | **Phase C reportee Sprint 44** | — | Switch des listeners onSnapshot pour lire depuis les sous-collections : refactor useData (5 listeners au lieu d'1) + adaptation des 60 tests E2E qui dependent du format `db = { dos: [...], tcs: [...] }`. A faire apres validation 7 jours de Phase A en prod. |
| | **Phase D reportee Sprint 45** | — | Cleanup : suppression des arrays dans le doc principal, suppression du code dual-write, refactor final des tests. A faire apres Phase C stable. |
| | **Verifications globales Sprint 43** | — | Build 20.74s, typecheck 0 erreur, 380/380 tests. Aucun changement visible utilisateur, aucune regression. Le miroir tournera silencieusement en prod et alimentera progressivement les sous-collections au fil des saves. **Effet metier** : (1) filet de securite contre la limite hard 1 MiB Firestore : meme si le doc explose, les sous-collections continueront de fonctionner; (2) prerequis technique de Phase C pose; (3) script backfill pret pour reconciliation des donnees existantes. |

---

## FAIT — Sprint 42 : Audit fond en comble - Phase 3 partielle (durcissement plateforme)

| # | Tache | Fichiers modifies | Details |
|---|-------|-------------------|---------|
| 173 | **F42.4 - Encodage UTF-8** | (verifie) | `file *.md` confirme : tous les .md sont deja UTF-8 valide. La seule occurrence "Ã©" trouvee dans `AUDIT-FOND-EN-COMBLE.md` etait une citation litterale entre backticks decrivant le probleme. Rien a corriger. |
| 174 | **F42.2 - Upgrade deps (jspdf, vite, rollup, protobufjs)** | `package.json`, `package-lock.json` | `npm audit fix` applique : jspdf 4.2.0 -> 4.2.1 (patch GHSA), vite 7.0.0-7.3.1 -> 7.3.2 (path traversal + WS + fs.deny), rollup 4.0.0-4.58.0 -> 4.59 (path traversal), protobufjs upgrade. 11 vulnerabilites -> 1 (xlsx, traitee en F42.5). |
| 175 | **F42.1 - Tests rules Firebase via emulateur** | `package.json` (+ `@firebase/rules-unit-testing`, +`@types/node`), `firebase.json` (config emulators), `src/__tests__/rules/firestore-security.test.ts` (CREE, 11 tests), `tsconfig.json` (add node types) | 11 tests valident les corrections de securite Sprint 40 F40.1 (P0.1 auto-adhesion) : refus auto-join admin sans invitation, refus inviteCode email-mismatch, refus elevation role via invitation, autorisation invitation valide, refus update users.companyId / users.role par self, refus invite creation par non-admin, refus acces fichier cross-tenant, refus squatting de companyId existant. Tests SKIPPED si l'emulateur n'est pas lance (comportement par defaut en CI). Scripts ajoutes : `npm run emulators` (lance Firestore + Storage emulator), `npm run test:rules` (FIRESTORE_EMULATOR_HOST=localhost:8080 vitest --run src/__tests__/rules). |
| 176 | **F42.3 - Migration fichiers vers Firebase Storage** | `src/fileStore.ts`, `src/firebase.ts` | `fileStore` refactor : `set/get/delete` essaie d'abord Firebase Storage (chemin `files/{companyId}/{fileId}`), fallback Firestore (legacy). Ancienne API preservee + bonus `getDownloadUrl()`. Compat ascendante : les fichiers deja stockes en Firestore continuent de fonctionner. Convention de chemin alignee avec `storage.rules` Sprint 40 (validation par membership). Note : Storage necessite plan Blaze pour les uploads, mais le fallback Firestore reste actif sur Spark. |
| 177 | **F42.5 - Remplacement xlsx par exceljs** | `package.json` (uninstall xlsx, install exceljs), `src/ImportExcel.tsx`, `src/utils/export.ts` | xlsx avait 2 vulnerabilites high sans fix disponible (Prototype Pollution GHSA-4r6h-8v6p-xvw6 + ReDoS GHSA-5pgg-2g8v-p4x9). Remplace par exceljs (^4.4.0, sans vulnerabilites connues). `ImportExcel.tsx` : nouveau adapter `readWorkbook(buf)` qui parse via `workbook.xlsx.load()` et retourne le meme format `{ SheetNames, Sheets[name] = rows }` pour rester compatible avec `parseWorkbook` existant. `utils/export.ts` : 3 fonctions reecrites (`exportDossiers`, `exportDepenses`, `exportFinancierClient`), passent en async (await `wb.xlsx.writeBuffer()` + download via Blob/URL.createObjectURL). Migre aussi `f.s === 'PAYE'` vers `isDepensePayee()` (Sprint 38C). Resultat audit npm : **0 vulnerabilites**. |
| | **F42.6 reporte Sprint 43** | — | Sous-collections Firestore + transactions : reporte au Sprint 43 dedie. `useData.ts` = 818 lignes / 43 sites Firestore, plus tous les hooks (useDossierActions, useConteneurActions, useDPWorldSync, useCMASync, useCarrierSync) + 60 tests E2E qui dependent du format `db = { dos: [...], tcs: [...] }`. Effort realiste 3-5 jours. Plan Sprint 43 : dual-write transitoire (ecrit ancien + nouveau modele en parallele) + script backfill incremental + refactor tests E2E + rollback possible. Le capteur Sprint 38E warn deja si le doc depasse 500 KB, donnant le temps pour la migration. |
| | **Verifications globales Sprint 42** | — | Build 24.26s, typecheck 0 erreur, 380/380 tests + 11 skipped (rules emulator). **Effet metier** : (1) **0 vulnerabilites npm** (avant : 11 dont 2 critiques); (2) tests rules Firebase prets a lancer via emulateur pour valider les corrections Sprint 40 (P0.1 auto-adhesion); (3) uploads de fichiers > 600 Ko desormais possibles via Storage (avec fallback Firestore pour Spark); (4) export Excel sans risque de prototype pollution/ReDoS; (5) pipeline `deploy = typecheck && build && firebase deploy` bloque tout deploy avec erreurs TS. |

---

## FAIT — Sprint 41 : Audit fond en comble - Phase 2 (stabilisation logique metier)

| # | Tache | Fichiers modifies | Details |
|---|-------|-------------------|---------|
| 166 | **F41.1 - Depenses : mt independant du statut paiement (P1.6)** | `src/components/dossiers/NDepForm.tsx`, `src/hooks/useAppMetrics.ts` | L'audit avait identifie que `mt` etait force a 0 quand `status !== "payee"` lors de la sauvegarde NDepForm. Resultat : les depenses "a_payer" avaient mt=0, faisant disparaitre les impayes des totaux (`totalDep`, `totalImpaye`) et donc des marges. Fix : `mt` = TTC saisi (ou HT en fallback), INDEPENDANT du statut. Le calcul "paye" reste filtre par `isDepensePayee()` (helper canonique Sprint 38C). `useAppMetrics` aussi migre vers `isDepensePayee` au lieu de `f.s === "PAYE"` (gere le nouveau `status` + le legacy `s`). |
| 167 | **F41.2 - Creation dossier : da vide par defaut (P2.11)** | `src/components/dossiers/NDosForm.tsx` | Avant : `var [da, sDa] = useState(... \|\| today())`. Resultat : un nouveau dossier prenait `da = today()` si on ne saisissait rien, et le TC etait cree directement en PORT meme si la vraie ETA n'etait pas connue. Apres : `da` defaut vide. Le transitaire doit saisir la vraie ETA. Si vide ou future, le TC reste en ATTENDU et le dossier en INITIALISE (via `deriveDossierStatus` Sprint 40). |
| 168 | **F41.3 - Edition dossier : reconciliation TC par numero (P2.12)** | `src/hooks/useDossierActions.ts` | Avant : `editDos()` supprimait tous les TC PORT/ATTENDU et les recreait avec de nouveaux IDs `mid()`. Resultat : perte de notes, dates `dsp`, champs DPWorld (`dpwSyncedAt`, `dpwConflict`, etc.), historique local pour chaque TC re-edite. Apres : reconciliation par numero `n`. Pour chaque TC existant : si son numero est dans le form, on garde l'ID + tous les champs, on met a jour seulement `ty` et `po`. Si absent du form et statut PORT/ATTENDU : supprime. Si en transit : conserve meme s'il a disparu du form (suppression interdite via edit). Nouveaux numeros : crees avec nouveau ID. Tests existants 60/60 toujours OK. |
| 169 | **F41.4 - Branchement reconcileDossierState dans toutes les syncs (P1.10)** | `src/hooks/useDPWorldSync.ts`, `src/hooks/useCMASync.ts`, `src/hooks/useCarrierSync.ts` | Critique audit point 10 : "Les syncs modifient dossier/TC mais ne passent pas toutes par le meme moteur d'invariants et de statut dossier". Resultat : une sync officielle peut corriger une `da` sans remettre le dossier dans un etat coherent (statut + TC). Fix : chaque save dans `syncDPWorld`, `syncAllDPWorld`, `syncCMA`, `syncCarrier` passe par `reconcileDossierState(newDos, newTcs)` avant le `sv()`. Si la sync a pose une `da` future et que des TC sont DISPATCHE/TRANSIT/etc., ils sont retrogrades en ATTENDU + statut dossier recalcule en INITIALISE. |
| 170 | **F41.5 - Matrice permissions UI/rules (P2.13)** | `src/App.tsx` | Avant : `canEdit = role === "admin" \|\| role === "agent"` mais firestore.rules autorisaient `update companies` pour `admin/editor/agent`. Un editor n'avait pas les boutons cote UI mais pouvait modifier via API. Fix : alignement UI sur rules : `canEdit = admin \|\| editor \|\| agent`. |
| 171 | **F41.6 - Unification invitations assignedEmail (P2.14)** | `src/useData.ts` | `addMemberByEmail()` ecrivait `forEmail` alors que `createInvite()` et les firestore.rules attendent `assignedEmail`. Les invites generees via "Ajouter membre" etaient donc ignorees par la validation rules. Fix : unification sur `assignedEmail` (normalise en lowercase trimme). |
| 172 | **F41.7 - Limite upload fichier 600 Ko (P1.7 quick fix)** | `src/components/dossiers/NDepForm.tsx` | Avant : limite UI 4 Mo, incompatible avec Firestore (limite hard 1 MiB par document) + le base64 grossit le payload de ~33%. Uploads frequemment instables ou impossibles. Quick fix : limite stricte 600 Ko (apres base64 ~800 Ko, marge OK). Long terme : migration vers Firebase Storage (Sprint 42). |
| | **F41.8 differe** | — | Tests rules Firebase via emulateur (`@firebase/rules-unit-testing`) : differe au Sprint 42 (necessite installation devDeps + lancement emulateur + script npm run test:rules). Le user a accepte ce report. |
| | **Verifications globales Sprint 41** | — | Build 29.03s, typecheck 0 erreur, 380/380 tests. **Effet metier** : (1) les impayes apparaissent enfin dans les totaux et marges; (2) plus de creation accidentelle de dossier EN_TRANSIT avec `da = today()`; (3) l'edition d'un dossier ne detruit plus l'historique et les donnees DPWorld des TC existants; (4) toutes les syncs (DPWorld, CMA, Carrier) reconcilient automatiquement les incoherences detectees par les invariants; (5) les editors voient enfin les boutons d'edition cote UI; (6) les invitations "Ajouter membre" fonctionnent enfin (champ aligne avec les rules); (7) plus de fichiers trop gros qui cassent l'enregistrement Firestore. |

---

## FAIT — Sprint 40 : Audit fond en comble - Phase 1 (P0 securite + invariants metier)

| # | Tache | Fichiers modifies | Details |
|---|-------|-------------------|---------|
| 159 | **F40.1 - Refonte rules Firestore + Storage (P0.1 auto-adhesion)** | `firestore.rules`, `storage.rules`, `src/useData.ts` | Demande utilisateur : "Rules Firestore + Storage + tests rules en mode bloquant, en premier". L'audit fond-en-comble a identifie une vulnerabilite critique : un utilisateur authentifie pouvait s'auto-greffer dans n'importe quelle company active, choisir son role, modifier `users.companyId` librement, et acceder a TOUS les fichiers Storage. Refonte complete : (a) `users/write` : update bloque modification de `companyId`/`role` par self (immutabilite sauf super-admin). (b) `companies/create` : autorise seulement si la company n'existe pas encore (1er createur). (c) `companies/delete` : super-admin uniquement. (d) `members/create` : exige super-admin OU `isFirstCompanyCreator` OU `hasValidInvite` (verification de l'invite par lookup `/invites/{code}` avec match companyId + assignedEmail + non utilisee + role correspondant). (e) `members/delete` : un admin ne peut pas se retirer lui-meme. (f) `invites/create` top-level : exige que le createur soit admin de la company concernee (avant : tout authentifie). (g) `tracking/write` : check membership en plus de `users.companyId` (defense en profondeur). (h) `files/*` : check `isMember()` + `users.companyId` (qui est desormais immutable). (i) `storage.rules` refonte : `match /files/{companyId}/{fileId=**}` autorise read/write seulement si membre de la company. Tout autre chemin interdit. (j) `useData.ts` joinWithCode : passe `inviteCode` dans le memberData pour que les rules valident. Helpers ajoutes : `isFirstCompanyCreator`, `hasValidInvite`. |
| 160 | **F40.2 - Fix typecheck + deploy bloquant** | `src/pages/SuperAdmin.tsx`, `src/services/__tests__/dpworld.test.ts`, `package.json` | 2 erreurs typecheck identifiees par l'audit : (a) `PaymentMethod` utilise sans import dans SuperAdmin.tsx ligne 83 (introduite Sprint 36) - ajout import. (b) `dpwSyncedAt` assigne sur un type sans la declaration - cast `mkTc` return type a `any`. `npm run deploy` modifie : `npm run typecheck && vite build && firebase deploy`. Plus aucun deploy ne passe avec des erreurs TypeScript. |
| 161 | **F40.3 - DossierStatus canonique (retire ACTIF, ajoute SECURISE/EN_TRANSIT)** | `src/domain/statuses.ts`, `src/domain/__tests__/statuses.test.ts` | L'audit a revele que mon Sprint 38A avait introduit `DossierStatus = 'INITIALISE' | 'ACTIF' | 'CLOTURE' | 'ARCHIVE'`, mais le RUNTIME utilise `INITIALISE | SECURISE | EN_TRANSIT | CLOTURE | ARCHIVE` (cf. `utils/dossierStatus.computeDossierStatus`). Mon `ACTIF` etait fictif. Correction : alignement sur les 5 vrais statuts. `normalizeDossierStatus` defaut passe de `'ACTIF'` (faux) a `'INITIALISE'` (neutre). 9 tests adaptes. |
| 162 | **F40.4 - Invariants metier centralises** | `src/domain/invariants.ts` (CREE), `src/domain/__tests__/invariants.test.ts` (CREE, 24 tests) | Sprint 40 priorite 1 (P0.2 audit) : source de verite unique pour les regles de transition TC et de calcul statut dossier. API : (a) `isDaFuture(dos)` predicat date arrivee future. (b) `canDispatchTc(tc, dos)` autorise dispatch (PORT->DISPATCHE) : verifie tc.st === 'PORT' + dos.da pas dans le futur + BAE/Pregate. (c) `canAdvanceTc(tc, dos, ns)` combine machine d'etat (Sprint 38B) + regle da pour PORT/DISPATCHE. (d) `deriveDossierStatus(dos, tcs)` calcule statut dossier en forcant INITIALISE si da future, peu importe tc.st. (e) `reconcileDossierState(dos, tcs, options)` reconcile dos + tcs : retrograde les TC PORT/DISPATCHE/etc. en ATTENDU si da future, recalcule statut dossier. 24 tests unitaires couvrant invariants purs + cas reel BUILDERS MALI (da future + TC DISPATCHE -> INITIALISE + retrogradage). |
| 163 | **F40.5 - Branchement dispatch / advance / computeDossierStatus sur invariants** | `src/hooks/useConteneurActions.ts`, `src/utils/dossierStatus.ts` | (a) `dispatch()` utilise `canDispatchTc(tc, d)` au lieu du check BAE/Pregate seul. Refus si da future. (b) `advance()` utilise `canAdvanceTc(tc, dos, ns)` au lieu de `canTcTransition` seul. Refus si da future pour PORT/DISPATCHE. (c) `computeDossierStatus` (utils/dossierStatus.ts) : ajout garde da-future en debut. Toute la chaine de decision passe maintenant par les invariants. |
| 164 | **F40.6 - Fix tracking public (DISPATCHE/RETURNED)** | `src/TrackingPage.tsx` | P1.4 audit : la page tracking utilisait `DISPATCH` et `RETOUR` au lieu des codes domaine `DISPATCHE` et `RETURNED`. Un TC dispatche ou retourne etait affiche au mauvais niveau cote client. Alignement sur `TC_STATUSES`. |
| 165 | **F40.7 - Tests scenario reel BUILDERS MALI (da future)** | `src/__tests__/scenarios/coherence-da-future.test.ts` (CREE, 7 tests) | Test regression sur le cas exact rapporte : BL LHV3990909, da = 2026-05-30 (+15j), TC DISPATCHE, dossier "EN_TRANSIT". Verifie que (a) `canDispatchTc` refuse, (b) `canAdvanceTc` refuse PORT et DISPATCHE, (c) `deriveDossierStatus` force INITIALISE, (d) `reconcileDossierState` retrograde tous les TC en ATTENDU + force dossier INITIALISE. Plus integration via hook : bouton Dispatch refuse, advance ATTENDU->PORT refuse, advance PORT->DISPATCHE refuse. |
| | **Verifications globales Sprint 40** | — | Build 12.01s, typecheck 0 erreur (avant : 2), lint 0 erreur (723 warnings non-bloquants), 380/380 tests (avant : 349 + 31 nouveaux). **Tests rules Firebase differes au Sprint 41** : necessitent `@firebase/rules-unit-testing` + emulateur Firestore lance. Plan : `npm run test:rules` qui orchestrera emulateur + tests. **Effet metier** : (1) faille critique d'auto-adhesion fermee : un utilisateur ne peut plus s'auto-greffer dans une company sans invitation valide; (2) impossible techniquement de passer un TC en PORT/DISPATCHE tant que le navire n'est pas arrive; (3) `deriveDossierStatus` evite les incoherences observees (EN_TRANSIT avec da future); (4) deploy bloque si typecheck KO; (5) tracking public aligne sur les vrais codes statut. **A noter** : un dossier deja en prod avec incoherence (da future + TC DISPATCHE) ne sera pas auto-corrige tant que `reconcileDossierState` n'est pas branche dans le flow (Sprint 41). En attendant le useEffect existant fait deja le job pour ATTENDU<->PORT. |

---

## FAIT — Sprint 39 : Tests E2E scenarios metier complets

| # | Tache | Fichiers modifies | Details |
|---|-------|-------------------|---------|
| 158 | **Sprint 39 - Tests E2E scenarios metier** | `src/__tests__/scenarios/helpers.ts` (CREE), `src/__tests__/scenarios/workflow-nominal.test.ts` (CREE), `src/__tests__/scenarios/detention-blocking.test.ts` (CREE), `src/__tests__/scenarios/state-machine-blocks.test.ts` (CREE) | Point R7 de la critique metier : "Ajouter des tests sur les scenarios metier complets : arrivee, dispatch, retour vide, detention, cloture, caution, paiement DPWorld". Couverture de 3 workflows E2E par chainage de `useAppLogic`. **helpers.ts** : `setupScenario(initialDb)` retourne `{ hook, saves, modals, rerender, getDb, lastSave, lastModal }`. Pattern d'utilisation : `act(() => hook.current.X()); s.rerender();` pour propager le dernier save dans le hook avant l'action suivante (le hook ne souscrit pas a Firestore, il prend `db` en prop). Helpers `getTcStatus(saves, tcid)`, `getDosStatus(saves, dosId)`, `daysAgo(n)`, `today()`. **Scenario 1 — workflow nominal** (2 tests) : creation dossier IMPORT + 1 TC PORT + BAE obtenu, patchDos(da) -> auto-stub Depenses (dpworld, compagnie_location, etc.), dispatch (PORT->DISPATCHE) avec chauffeur, advance TRANSIT, advance BAMAKO, advance RETURNED -> cloture auto. Test avec 2 TC : cloture attend que LES DEUX soient RETURNED. **Scenario 2 — detention bloquante** (3 tests) : (a) detention 15j > franchise 4j -> modale `t: detention` ouverte avec `{ did, jours, depassement, franchise }`, dossier reste ouvert ; (b) detention exactement === franchise -> cloture OK (tolerance limite) ; (c) region CORRIDOR (Bamako) avec default 23j -> 20j detention OK. **Scenario 3 — machine d'etat** (6 tests) : (a) RETURNED -> TRANSIT refuse (regression), (b) PORT -> BAMAKO direct refuse (saut DISPATCHE+TRANSIT), (c) ATTENDU -> DISPATCHE refuse, (d) PORT -> ATTENDU refuse, (e) TRANSIT -> BAMAKO autorise (raccourci tolere), (f) BAMAKO -> KATI autorise (retour). Validation faite via `canTcTransition` Sprint 38B + `advance()` qui return early avec nf error si transition invalide. **Verifications** : 11 tests scenarios, build 10.64s, lint 0 erreur, 349/349 tests total (338 + 11). **Effet metier** : (1) garantie que les workflows critiques (arrivee, dispatch, retour, cloture, detention) restent fonctionnels apres chaque refactor, (2) regression-proof sur la machine d'etat (impossible de re-introduire un bug PORT->BAMAKO direct), (3) regression-proof sur la cloture auto qui doit attendre tous les TC RETURNED ET respecter la franchise detention. |

---

## FAIT — Sprint 38 : Robustesse metier (quick wins post-audit)

| # | Tache | Fichiers modifies | Details |
|---|-------|-------------------|---------|
| 153 | **38E - Capteur taille document Firestore** | `src/useData.ts` | Demande utilisateur : "on attaque les quicks wins" suite a la critique metier. Ajout d'un `console.warn` dans `save()` si la taille serialisee de `clean` depasse 500 KB. Limite hard Firestore = 1 048 576 bytes (1 MiB) ; on alerte a 500 KB pour anticiper une migration vers sous-collections bien avant l'erreur bloquante "Document exceeds the maximum size". Format du warn : `companyId=X taille=N KB (M bytes). Limite hard = 1024 KB. Envisager migration en sous-collections.`. Defensif `try/catch` (sanitize() doit garantir la serialisabilite, mais ceinture/bretelles). |
| 154 | **38D - Consolidation calculs franchise (source unique)** | `src/utils/franchise.ts`, `src/hooks/useConteneurActions.ts`, `src/utils/date.ts` | Critique point 8 : "tcFranchise dans useConteneurActions et calcAlertesFranchise dans utils/date.ts dupliquent les regles `joursPort = b - startSur + 1`, gestion BAD (`bs === 'OBTENU' && bv`), clamp negatif". Solution : ajout de 2 helpers canoniques dans `franchise.ts` : (a) `joursSurestariesPort(d, dateFin?)` : decompte depuis bv si BAD obtenu sinon da, j+1 inclusif, clamp 0 ; (b) `joursDetention(dsp, dr?)` : decompte sortie terminal -> retour vide, j+1 inclusif. `tcFranchise` (useConteneurActions) et `calcAlertesFranchise` (utils/date) appellent maintenant ces helpers. Si demain on change la regle (ex: passer en exclusif), on le fait a un seul endroit. Tests existants : 306/306 passent (memes regles, donc memes resultats). |
| 155 | **38B - Machine d'etat TC + validation transitions** | `src/domain/tcStateMachine.ts` (CREE), `src/domain/__tests__/tcStateMachine.test.ts` (CREE), `src/hooks/useConteneurActions.ts` | Critique point 5 : "`advance(tid, ns)` accepte n'importe quel ns string. Il faudrait empecher PORT->BAMAKO sans dispatch, RETURNED->TRANSIT, etc." Solution : (a) `src/domain/tcStateMachine.ts` definit la table des transitions valides (ATTENDU->PORT->DISPATCHE->TRANSIT->KATI->BAMAKO->RETURNED + raccourcis tolerees TRANSIT->BAMAKO, BAMAKO->KATI au retour, DISPATCHE->RETURNED dossier Dakar). Helper `canTcTransition(from, to)` retourne `{ valid, reason? }`. Helper `isTerminalStatus(status)`. Constante `TC_STATUSES` (liste canonique). (b) `useConteneurActions.advance()` valide la transition avant d'appliquer le patch : si refusee, `nf('Transition refusee : ' + reason, 'error')` et return. (c) 23 tests unitaires couvrant : cycle nominal, raccourcis toleres, transitions interdites (RETURNED->autre, PORT->BAMAKO direct, regression PORT->ATTENDU), cas limites (creation sans statut, no-op, legacy non-standard, statut cible inconnu). |
| 156 | **38C - Unification Depense.s / status + script migration** | `src/utils/depenseStatus.ts` (CREE), `src/types.ts`, `scripts/migrate-depenses-legacy-status.mjs` (CREE) | Critique point 7 : "deux statuts paiement : legacy `s: PAYE/ATT` et nouveau `status: en_attente_facture/a_payer/payee`. Risque d'incoherences. Choisir une source de verite." Solution : (a) `src/utils/depenseStatus.ts` definit `getDepenseStatus(dep)` (resolution canonique vers `status`, fallback via guessStatus si `status` absent), `isDepensePayee`, `isDepenseAPayer`, `isDepenseEnAttenteFacture`, `statusToLegacyS` (conversion inverse pour retrocompat pendant migration). (b) `types.ts` : annotation `@deprecated` sur `Depense.s` avec pointeur vers depenseStatus.ts. (c) `scripts/migrate-depenses-legacy-status.mjs` : script Node Firebase Admin qui parcourt toutes les companies, pour chaque depense sans `status` : map `s === 'PAYE'` -> `payee`, `s === 'ATT'` -> `a_payer`, defaut `a_payer`. Mode dry-run par defaut, `--apply` pour ecrire. Aucun fichier consommateur de `dep.s` (24 fichiers) n'a ete touche : migration progressive sans risque, le helper continue de marcher sur les donnees legacy non-migrees. |
| 157 | **38A - Statuts canoniques + helpers normalize** | `src/domain/statuses.ts` (CREE), `src/domain/__tests__/statuses.test.ts` (CREE) | Critique point 4 : "Conteneur.st, Dossier.st acceptent `\| string`. Affaiblit la logique metier : faute de frappe peut casser calculs, alertes, filtres." Solution pragmatique (sans casser la retrocompat) : (a) `src/domain/statuses.ts` definit les types canoniques `DossierStatus` ('INITIALISE' | 'ACTIF' | 'CLOTURE' | 'ARCHIVE'), reexporte `TcStatus` depuis tcStateMachine. Constantes `DOSSIER_STATUSES`. Helpers `normalizeTcStatus(value, fallback?)` et `normalizeDossierStatus(value, fallback?)` qui ramenent toute valeur hors-spec a une valeur safe. Type guards `isKnownTcStatus`, `isKnownDossierStatus`. (b) 9 tests unitaires couvrant statuts valides, valeurs invalides (null/undefined/nombre/string inconnue/casse), fallback explicite, type guards. (c) `types.ts` conserve `\| string` dans les unions pour ne pas casser massivement le code existant (les statuts legacy en base ne sont pas tous canoniques). Les nouveaux calculs metier passeront par les helpers de normalisation. |
| | **Verifications globales Sprint 38** | — | Build 15.42s, lint 0 erreur (679 warnings non-bloquants), 338/338 tests (306 anciens + 23 tcStateMachine + 9 statuses). **Effet metier** : (1) anticipation du bottleneck 1 MiB Firestore via warn console proactif, (2) plus de divergence de calcul franchise entre Tcs et alertes Accueil, (3) blocage des regressions de statut TC (RETURNED ne peut plus revenir en TRANSIT), (4) source de verite claire pour le statut paiement avec migration prete a executer, (5) helpers de normalisation prets pour proteger les calculs metier contre les valeurs hors-spec. Critique 6 fichiers nouveaux + 4 fichiers patches, 0 fichier supprime. |

---

## FAIT — Sprint 37 : Gestion des super-admins (lister, ajouter, retirer)

| # | Tache | Fichiers modifies | Details |
|---|-------|-------------------|---------|
| 152 | **Sprint 37 — UI de gestion des super-admins** | `src/pages/SuperAdmin.tsx` | Demande utilisateur : "je veux accorder ce role a un autre mail" + choix option B "Ajouter une UI Sprint 37". Avant : pour donner l'acces super-admin a une nouvelle personne, il fallait passer par Firebase Console > Firestore > creer manuellement un doc `/superAdmins/{uid}`. Apres : nouvelle section "Super-admins" en bas de la page SuperAdmin (juste avant la cle Gemini) qui liste les comptes actifs et permet d'ajouter/retirer en quelques clics. **Section UI** : entete avec compteur ("N comptes avec acces super-admin") + bouton "+ Ajouter". **Form ajout depliable** : 3 champs (UID Firebase 28 caracteres en placeholder, Email, Role radio owner/admin). Validations cote front : UID >= 20 chars, email contient @ et ., pas de doublon UID. Cree le doc `/superAdmins/{uid}` via setDoc avec `email`, `createdAt: now`, `role`, `createdBy: super-admin courant`. **Liste** : pour chaque super-admin : avatar lettre, email (+ "(toi)" si c'est l'utilisateur courant), UID monospace, badge role colore (owner=purple, admin=info), date de creation, bouton "Retirer" rouge (sauf pour soi-meme). **Protections** : (a) impossible de se retirer soi-meme (evite le verrouillage accidentel), (b) impossible de retirer le dernier super-admin (un seul restant = pas de suppression), (c) `window.confirm()` avant retrait. **Rules Firestore** : deja OK depuis Sprint 34 (read/write `/superAdmins/{uid}` reserve aux super-admins, plus auto-lecture du doc personnel pour le AuthGate). **Fonctions ajoutees** : `loadSuperAdmins()` (charge la collection au mount), `addSuperAdmin()` (cree le doc + refresh state), `removeSuperAdmin(uid, email)` (delete + protections + confirm). **Verifications** : build 14.72s, lint 0 erreur (677 warnings non-bloquants), 306/306 tests. **Effet metier** : le super-admin peut desormais deleguer le role a un associe ou retirer un acces obsolete sans toucher a la console Firebase. Pratique pour la phase de developpement multi-personnes et pour la securisation si un compte super-admin doit etre revoque rapidement. |

---

## FAIT — Sprint 36 : Super-admin actions manuelles (plan, trial, suspension, notes)

| # | Tache | Fichiers modifies | Details |
|---|-------|-------------------|---------|
| 151 | **Sprint 36 — Actions super-admin sur BillingProfile** | `src/pages/SuperAdmin.tsx` | Demande utilisateur : "pour un debut je veux quelque chose de simple sans la gestion des paiements car n'ayant pas de client payant juste des beta testeurs". Extension du panel "Abonnement" en lecture seule (Sprint 35) avec actions inline pour le super-admin. **Actions disponibles** : (a) **Changer plan** : 3 boutons radio trial / standard / pro, le bouton actif est mis en surbrillance, click ecrit immediat dans `/companies/{id}/billing/profile`. (b) **Prolonger essai** : boutons +14j et +30j qui calculent `trialEndsAt = today + N` (format ISO YYYY-MM-DD) et remettent `billingStatus: 'trial'`. (c) **Suspendre / Reactiver** : bouton rouge "Suspendre" avec `window.confirm()` de securisation (rappel : la suspension bloque les writes Firestore via la rule `isCompanyActive()` Sprint 34D), bouton vert "Reactiver" sans confirmation qui remet `billingStatus: 'trial'`. (d) **Notes internes** : textarea 3 lignes editable inline, draft local dans `notesDraft[companyId]`, bouton "Enregistrer" + "Annuler" qui apparaissent seulement quand le contenu a change. **Fonction core `updateBilling(companyId, updates)`** : merge avec le profil existant + defauts (`billingStatus: 'trial'`, `plan: 'trial'`, `paymentMethod: 'manual'`) si pas de profile, set automatique `updatedAt: now` + `updatedBy: user.email` (super-admin courant), `setDoc(..., merged, { merge: true })` puis refresh local du state `billing` pour affichage immediat sans round-trip. State `saving[companyId]` pour disable les boutons pendant l'ecriture et eviter les double-clicks. Affichage en haut du panel : `trialEndsAt`, derniere maj (date + email). **Scope volontairement reduit** : pas de gestion paiement (`lastPaymentAt`, `paymentMethod`, `subscriptionEndsAt`), pas d'audit trail dedie, pas de modal custom (window.confirm natif suffit en mono-super-admin). Si besoin futur : ajout sous-collection `/companies/{id}/billing/log/{logId}` ou Stripe webhook. **Verifications** : build 13.63s, lint 0 erreur (673 warnings non-bloquants), 306/306 tests. **Effet metier** : le super-admin peut desormais gerer le cycle de vie beta des entreprises (changer leur plan, prolonger les essais, suspendre/reactiver l'acces, prendre des notes commerciales) directement depuis l'UI Sapurai, sans console Firebase. Aligne avec la phase actuelle "beta testers, pas de paiement" et extensible quand un vrai modele commercial sera valide. |

---

## FAIT — Sprint 35 : Super-admin lecture seule

| # | Tache | Fichiers modifies | Details |
|---|-------|-------------------|---------|
| 150 | **Nettoyage UID hardcode** | `firestore.rules` | Suppression definitive de `T9IfzQNpaCTnl0D3qOKC1D7MmCs2` des regles Firestore. `isSuperAdmin()` ne verifie plus que `/superAdmins/{uid}`. |
| 149 | **Routing super-admin via Firestore** | `src/main.tsx` | Remplacement de `VITE_SUPER_ADMIN_UID` (env var) par la verification du doc `/superAdmins/{uid}` dans Firestore. Nouveau composant `AuthGate` qui verifie le statut super-admin apres auth, affiche SuperAdmin ou AuthenticatedApp. Cache en memoire (`SUPER_ADMIN_CACHE`). |
| 148 | **Page SuperAdmin lecture seule** | `src/pages/SuperAdmin.tsx` (CREE) | Nouvelle page remplacant l'ancien AdminPanel. KPIs (actives/essai/suspendues/total). Liste entreprises avec nom, ID, statut (badge couleur), plan, nombre dossiers/TC/chauffeurs/depenses, derniere activite. Panel "Abonnement" depliable affichant billing profile. Panel "Membres" depliable avec avatar, nom, email, role, date d'arrivee. Lecture seule : pas de toggle status, pas de suppression, pas de modification. Cle Gemini affichee en read-only. |
| 147 | **Auto-lecture /superAdmins/{uid}** | `firestore.rules` | Regle `allow read: if isSuperAdmin() || (isAuth() && request.auth.uid == uid)` pour permettre a un utilisateur de verifier son propre statut super-admin sans droits super-admin preexistants. |

---

## FAIT — Sprint 34 : Securite Firestore + super-admins + billing + suspension

| # | Tache | Fichiers modifies | Details |
|---|-------|-------------------|---------|
| 143 | **34A — Migration isSuperAdmin() vers /superAdmins/{uid}** | `firestore.rules` | `isSuperAdmin()` ne checke plus seulement un UID hardcode (`T9IfzQNpaCTnl0D3qOKC1D7MmCs2`), mais d'abord l'existence d'un document dans `/superAdmins/{uid}`. L'UID hardcode reste en fallback temporaire pour la migration, sera supprime au Sprint 35. Helpers ajoutes : `isAuth()`, `isMember(companyId)`, `memberHasRole(companyId, roles)`. |
| 144 | **34B — Collection /superAdmins + type associé** | `firestore.rules`, `src/types.ts` | Nouveau match `/superAdmins/{uid}` avec `allow read, write: if isSuperAdmin()` (seed manuel Firebase Console). Nouveau type `SuperAdminProfile` dans types.ts : `{ email, createdAt, role: 'owner' | 'admin' }`. |
| 145 | **34C — Types billing + sous-collection + migration** | `firestore.rules`, `src/types.ts`, `scripts/migrate-billing-legacy.mjs` (CREE) | Types ajoutes : `BillingStatus` (trial/active/past_due/suspended), `PlanType` (trial/standard/pro), `PaymentMethod` (manual/wave/bank_transfer/cash/stripe), `BillingProfile` complet. Nouveau match `/companies/{companyId}/billing/{billingDoc}` : lecture par membres + super-admin, ecriture super-admin uniquement. Script de migration `scripts/migrate-billing-legacy.mjs` (--dry supporte) pour initialiser les companies legacy en `trial` + 30j. |
| 146 | **34D — Suspension bloquante dans les rules** | `firestore.rules` | Helper `isCompanyActive(companyId)` verifie que `billingStatus != 'suspended'` (ou billing absent = actif). Applique a tous les writes sauf super-admin : companies (update/delete), members (create/update/delete), notifications (create/update), invites (create/delete), top-level invites (create), tracking (write), files (create/update/delete). Rating public non-authentifie reste autorise (pas de suspension check). |

---

## FAIT — Lot 1 : Fiabilisation Sync DPWorld (par TC, conflits, predicat individuel)

| # | Tache | Fichiers modifies | Details |
|---|-------|-------------------|---------|
| 138 | **Lot 1 — Fiabilisation Sync DPWorld par TC** | `src/types.ts`, `src/services/dpworld.ts`, `src/services/__tests__/dpworld.test.ts`, `src/hooks/useDPWorldSync.ts`, `src/hooks/__tests__/useDPWorldSync.test.ts`, `src/hooks/useAppLogic.ts`, `src/components/shared/AppModals.tsx`, `src/components/dossiers/DetView.tsx`, `src/App.tsx`, `CHANGELOG-AUDIT.md` | Correction du bug racine : l'app ne verifiait pas DPWorld si tous les TC etaient localement marques `DISPATCHE/TRANSIT/KATI/BAMAKO/RETURNED`. Le predicat de skip sync utilisait `tc.st` local au lieu de la verite DPWorld confirmee. **1. Champs dpw\* sur Conteneur** (`types.ts`) : `dpwAta`, `dpwDischarge`, `dpwTimeIn`, `dpwTimeOut`, `dpwVisitState`, `dpwSyncedAt`, `dpwConflict`. Stockage de la verite armateur par TC, independamment du statut metier. **2. Split du mapping DPWorld** (`services/dpworld.ts`) : `mapTcDPWorld(tc, dpTc)` pour un TC individuel (transition directe ATTENDU→DISPATCHE si timeOut+3DEPARTED, pas de regression de statut), `mapDossierDPWorld(dpData, dos)` pour les champs dossier (da, bs, bv, as2, bd, pn), `detectTcConflict(tc, dpTc)` pour detecter STATUS_MISMATCH / MISSING_DSP / NOT_FOUND. `DPWorldPatches` enrichi d'un champ `conflicts[]` remonte dans le flux sync. **3. Nouveau predicat `needsDPWorldSync`** (`useDPWorldSync.ts`) : base uniquement sur `tc.dpwVisitState`, `tc.dpwTimeOut`, `tc.dpwConflict`, `tc.dpwSyncedAt`. Plus jamais de reference a `tc.st`. Le skip global "TC deja sortis du port" est supprime. **4. Nouvelle action `syncTcDPWorld(tcid, opts?)** : synchronisation chirurgicale d'un TC individuel par son numero (requete API par TC). Disponible dans l'UI via bouton "🔄 Resync DPWorld" par TC dans `DetView.tsx`. Option `{ force: true }` pour ignorer le predicat. **5. Detection et persistance des conflits** : `STATUS_MISMATCH` (local sorti mais DPWorld pas 3DEPARTED), `MISSING_DSP` (DISPATCHE sans dsp), `NOT_FOUND` (TC absent de la reponse DPWorld). Stockes dans `tc.dpwConflict` et effaces automatiquement quand le conflit est resolu. **6. UI** : bouton "Resync TC" ajoute dans `DetView.tsx` par TC, prop `syncTcDPWorld` passee via `AppModals.tsx`. Tests : 306/306 verts. Build : OK. Lint : 0 erreur. **Effet metier** : un TC localement DISPATCHE mais non confirme par DPWorld est desormais re-synchronisable, detecte comme conflit, et peut etre reset. Plus de blocage silencieux "TC deja sortis du port — sync DPWorld inutile". |

---

## FAIT — Sprint 33 : Migration Scan BL Gemini -> Cloudflare Workers AI (free tier)

| # | Tache | Fichiers modifies | Details |
|---|-------|-------------------|---------|
| 137 | **Reactivation Scan BL via Cloudflare Workers AI (LLaVA 1.5 7B)** | `workers/scan-bl-proxy/{worker.js, wrangler.toml, README.md}` (nouveaux), `src/ScanBL.tsx`, `src/components/dossiers/NDosForm.tsx`, `src/components/shared/AppModals.tsx` | Demande utilisateur : "existe t'il une alternative gratuite a l'api de gemini" + "oui pour le cloudflare, on test pour la compagnie de test". Le Sprint 27 avait desactive Scan BL parce que le Senegal est hors free tier Gemini (erreur 429 quota), mais le besoin metier est reel : un transitaire qui scanne un BL plutot que de retaper 6+ champs gagne ~2 minutes par dossier. (1) **Nouveau Cloudflare Worker `scan-bl-proxy`** suivant le pattern de `cma-proxy` (CORS whitelist sapurai.app + sapurai-84984.web.app + localhost dev, JSON response, error handling defensif). Endpoint `POST /scan` qui recoit `{ image: base64/dataURL, mimeType }`, convertit en `Uint8Array`, appelle `env.AI.run('@cf/llava-hf/llava-1.5-7b-hf', { prompt, image, max_tokens: 1024 })` via le binding Workers AI (defini dans wrangler.toml `[ai] binding = "AI"`), parse la reponse texte (tolere backticks markdown + texte parasite, isole le bloc JSON via `firstBrace`/`lastBrace`), normalise defensivement les champs (bl/client/compagnie/date_arrivee/contact/conteneurs[]) et retourne `{ ok: true, data, model, fetchedAt }`. Variable optionnelle `SCAN_MODEL` pour switcher de modele sans redeployer (alternatives : `@cf/meta/llama-3.2-11b-vision-instruct`, `@cf/unum/uform-gen2-qwen-500m`). (2) **`src/ScanBL.tsx` reecrit** : appel `https://scan-bl-proxy.sapurai.workers.dev/scan` au lieu de l'API Gemini publique. Plus besoin du prop `apiKey` (le Worker gere l'AI via binding interne, l'utilisateur final n'a plus a configurer de cle Gemini dans Parametres). Interface `ScanBLProps` simplifiee a `{ onResult }`. Touch targets `minHeight: 44` ajoutes sur les 3 boutons (Choisir/Rescanner/Utiliser) conformes WCAG 2.5.5. Gestion d'erreur amelioree : tente `response.json()` avec fallback message clair si Worker repond du non-JSON. (3) **`NDosForm.tsx` reactivation** : `var canScan = false && isBetaCompany(...) && !!p.apiKey` -> `var canScan = isBetaCompany(p.companyId) && !p.init`. Le gate `isBetaCompany` (BETA_COMPANIES = ['c_mocpodna9egt']) restreint le bouton scan a la compagnie test "General Transit Dakar" tant que le quota Workers AI n'est pas valide en production. Suppression de la prop `apiKey` dans l'usage `<ScanBL ... />`. (4) **`AppModals.tsx` modal scan** : import `isBetaCompany`, gate change de `cfg.geminiKey` (cle Gemini configuree) a `isBetaCompany(companyId)` (gate beta company), suppression de `apiKey={cfg.geminiKey}` dans le composant. **Avantages Cloudflare Workers AI** : (a) 10 000 neurons/jour gratuits = ~50-100 scans BL/jour (largement suffisant pour la beta), (b) edge computing proche Dakar (~80 ms vs 300+ ms vers serveurs Gemini US), (c) coherence architecture (Sapurai a deja 4 Workers : cma-proxy, dpworld-proxy, carrier-proxy, fcm-proxy), (d) pas de cle a exposer cote front, (e) pas de billing a activer, (f) si quota depasse, le Worker retourne 502 et l'app affiche le message d'erreur sans casser le flux NDosForm (le user peut toujours saisir manuellement). **Deploiement** : `cd workers/scan-bl-proxy && wrangler deploy` puis `npm run deploy` Firebase. **Verifications** : build, lint, tests. **Effet metier** : retour de la fonctionnalite scan BL (gain ~2 min/dossier) sans dependre de la disponibilite Gemini en region Senegal, et sans cout pour Sapurai tant que < 10k neurons/jour. **Fix post-deploiement (2026-05-09)** : Workers AI / LLaVA n'accepte pas les PDF (erreur 3010 'Unsupported image data'), seulement les images. Ajout de `pdfjs-dist` (lazy chunk 415 KB / 123 KB gzip) + helper `pdfToJpegDataUrl()` dans `ScanBL.tsx` qui convertit la page 1 du PDF en canvas (echelle 2x pour lisibilite OCR) puis exporte en JPEG qualite 85% avant envoi au Worker. Le bundle main n'est pas impacte (dynamic import). URL Worker mise a jour avec le subdomain reel `ozmanm10.workers.dev`. | **Sprint 33 v2 (2026-05-11)** : pivote architecture vers pipeline OCR + LLM text-only. Tests reels sur 3 BL (CMA CGM, Grimaldi, MSC) ont montre que Llama 3.2 Vision 11B hallucine systematiquement les codes alphanumeriques (BL number, container number, gross weight vs tare) - defaut intrinseque des modeles vision pour OCR precis. Solution : ajout `tesseract.js` (langues eng+fra, lazy chunk) cote front qui produit un texte OCR brut via vraie OCR (95%+ precision), puis envoi du texte au Worker qui utilise `@cf/meta/llama-3.1-8b-instruct` (text-only, beaucoup plus precis pour suivre des instructions structurees, 5-10x moins cher en neurons que Vision). Refonte `src/ScanBL.tsx` avec stages UX (PDF/OCR/AI) + progress Tesseract. Refonte `worker.js` avec 2 paths (text recommande, image legacy) et helper `readAiResponse()` robuste. Worker prompt durci : 7 regles strictes anti-hallucination (consignee != shipper != notify, gross != tare, codes alphanumeriques sans deviner). Garde-fou poids cote front : detection format EU/US + division si >35t. |

---

## FAIT — Sprint 32 : Page Conteneurs — Recherche TC + durees port/transit + dropdown tri

| # | Tache | Fichiers modifies | Details |
|---|-------|-------------------|---------|
| 136 | **3 ameliorations metier page Conteneurs** | `src/pages/Tcs.tsx` | Demande utilisateur pour rendre la page Conteneurs plus utile au quotidien. (1) **Filtre "Filtrer par client" remplace par "Recherche par n° TC"** : `qrCl` (filter dossier.cl) supprime au profit de `qrTc` (search direct dans `tc.n`, uppercase auto, fontFamily mono). Beaucoup plus utile : retrouver instantanement un n° TC parmi 76 TC affiches plutot que filtrer par client (deja faisable via page Dossiers). Placeholder explicite "Rechercher un n° TC (ex: MEDU482...)". (2) **Durees metier port + transit affichees pour chaque TC** : nouveau helper `tcDurations(tc, d)` qui calcule `portDays` (jours arrivee navire -> chargement camion via `d.da` -> `tc.dsp`) et `transitDays` (jours chargement -> retour vide via `tc.dsp` -> `tc.dr`). Etats `portInProgress` / `transitInProgress` quand le TC est encore au port ou en transit (calcul depuis aujourd'hui, suffix `j+`). Helper `tcDurationsLabel(dur)` pour format compact "8j P · 12j T" (ou "5j+ P" si en cours). Affichage dans 4 vues : (a) **Vue Kanban** : 4e ligne dans chaque card sous le type/franchise, font mono compact 10px. (b) **Vue Table desktop** : nouvelle ligne sous le n° TC dans la cellule Conteneur (mono 10px, text-muted). (c) **Vue Mobile row** : nouvelle ligne apres "type · poids · chauffeur" (mono 10px). (d) **Panel deplie** : 4e card "DUREE" dans la grille Details (passage `1fr 1fr 1fr` -> `1fr 1fr 1fr 1fr`), affichage detaille "8j au port" + "12j transit" en mono 11px, fallback "---" si aucune date dispo. (3) **Dropdown tri explicite** ajoutee a cote des inputs de recherche : 7 options (Priorite urgences, Date arrivee recent/ancien, Client A-Z/Z-A, N° TC A-Z, Poids lourd-leger). Mobile-friendly et coherent avec les colonnes cliquables desktop conservees. Bandeau "Tri par X" redondant retire (la dropdown rend le tri actif explicite). **Verifications** : 286/286 tests, build 11.86s, lint 0 erreur. **Effet metier** : (a) recherche TC instantanee (cas frequent : agent au port qui cherche son TC dans une liste de 50+), (b) visualisation des durees critiques pour anticiper surestaries (TC qui passe 10j+ au port = signal alerte) et detentions (TC qui traine 20j+ en transit = probleme), (c) tri controlable mobile sans clic sur header tableau. |

---

## FAIT — Sprint 31 : Polish design system global (borderRadius + touch targets + transitions)

| # | Tache | Fichiers modifies | Details |
|---|-------|-------------------|---------|
| 135 | **Polish design system global** | 38 fichiers (.tsx) src/ + CHANGELOG | Resolution des 3 drifts systemiques identifies par /impeccable audit + critique global. (1) **Migration borderRadius globale** : 174 occurrences hors-tokens migrees vers les 4 valeurs canoniques DESIGN.md (`--radius-sm` 6px, `--radius` 8px, `--radius-lg` 12px, `999` pill). Heuristique : 1/2/3/4/5 -> 6 (compact), 7/10 -> 8 (boutons/badges), 14/16 -> 12 (cards), 20/99 -> 999 (pills). Distribution finale : **uniquement 4 valeurs canoniques** (8 x246, 6 x189, 12 x70, 999 x15). Plus aucun radius hors-token dans le projet. (2) **Touch targets WCAG 2.5.5** : 17 boutons interactifs migres a `minHeight: 44` sur 5 pages (Dep x7, Tcs x4, AgentView x3, Stats x1, Dos x2). Pages mobile-critiques (Dep, Tcs, AgentView) particulierement importantes pour le transitaire en deplacement. (3) **`transition: "all"` x3** dans Caut.tsx (lignes 55-57, tabs PERMANENTES/LOUEES/VENDUES) remplaces par proprietes explicites `background 0.2s ease-out, color 0.2s ease-out, border-color 0.2s ease-out` (respect skill impeccable "Don't transition all"). (4) **Cleanup** : 0 console.log en prod, TODO commentaires justifies (references decisions documentees), warnings unused-vars pre-existants laisses (pas critiques, decision separee). **Verifications** : 286/286 tests verts, build 11.01s, lint 0 erreur. **Score audit attendu** : Theming 3/4 -> 4/4, A11y 3/4 -> 4/4, Consistency Nielsen 4 (2.5/4 -> 3.5/4), Aesthetic Nielsen 8 (3/4 -> 3.5/4). **Score moyen pages app** : 16.7/20 -> ~18.5/20. **Effet utilisateur** : ryhme visuel uniforme sur toute l'app (cards/buttons/badges coherents), boutons mobile cliquables sans rater (44x44 minimum), transitions fluides ease-out. |

---

## FAIT — Sprint 30 : Contexte impeccable officiel (PRODUCT.md + DESIGN.md + sidecar)

| # | Tache | Fichiers modifies | Details |
|---|-------|-------------------|---------|
| 134 | **Officialisation contexte design (skill impeccable)** | `PRODUCT.md` (CREE), `DESIGN.md` (CREE), `.impeccable/design.json` (CREE) | Suite a `/impeccable teach` + `/impeccable document` : creation des 3 fichiers de contexte officiel pour aligner les futures commandes impeccable (audit, critique, polish, bolder, etc.) sur le brand reel Sapurai. **PRODUCT.md** (6.7 KB) : stratégique, format teach.md strict. Register=`product`, users (transitaires Dakar B2B, profil reticent au changement, multi-device, Sahel 4G), product purpose (anticiper surestaries, sync DPWorld+CMA, tracking client), brand personality "Fiable. Robuste. Anticipateur.", tone rassurant pas revolutionnaire, anti-pattern principal "si l'utilisateur doit apprendre Sapurai >5 min c'est rate", 6 design principles strategiques (source verifiee, armateur fait foi, anticiper pas constater, le client voit sans appeler, desktop ET mobile parite, friction zero), a11y WCAG AA + offline critique + low-end Android. **DESIGN.md** (20 KB) : visuel, format Google Stitch (frontmatter YAML + 6 sections). Frontmatter : 11 colors descriptives (cream-office, ink-primary, green-sapurai, red-surestarie, amber-warehouse...), 6 typography roles (display/headline/title/body/label/mono-id), 4 rounded (sm/md/lg/pill), 6 spacing, 12 components (button-primary/ghost/success/export-outline, card-primary/section, input-text, badge-success/danger/amber, pill-toggle). 6 sections markdown : Overview avec Creative North Star "The Field Terminal" (terminal de terrain, pas SaaS premium), Colors avec 4 roles + 3 Named Rules (Cream-First, Sémantique Réservée, Pas de #000), Typography avec 3 Named Rules (Identifier Mono, Uppercase Label, 65-75ch Body), Elevation avec 2 Named Rules (Flat-By-Default, Pas de Glassmorphism), Components (buttons/cards/badges/inputs/nav/stepper/logo), Do's and Don'ts (10 Do's + 13 Don'ts forceful, citent les anti-patterns Sprint 28). **.impeccable/design.json sidecar** (7 KB, schemaVersion 2) : 11 colorMeta avec tonal ramps 8 steps, 6 typographyMeta avec purposes, 5 shadows tokens semantiques, 3 motion tokens (ease-out-quart, transition-state, transition-card), 3 breakpoints, 9 components avec HTML/CSS self-contained drop-in (Primary Button, Ghost Button, Success Button, Export Outline, Card Primary, Mono Identifier, Status Badge, Input Text, Sapurai Logo SVG inline). narrative complet (northStar, overview, keyCharacteristics, 8 rules, dos/donts) pour rendering panel impeccable. **Pas de modification code source** : pure documentation. Tests 286/286 inchanges. **Effet attendu** : les futurs `/impeccable audit/critique/polish/bolder/etc.` charge automatiquement ces 3 fichiers et calibre ses recommandations sur le brand reel (palette creme, mono identifiers, vert/rouge/ambre semantiques, pas de gradients hors-systeme). |

---

## FAIT — Sprint 29 : Harden TrackingPage (touch targets WCAG + h1 sr-only + empty states + truncate)

| # | Tache | Fichiers modifies | Details |
|---|-------|-------------------|---------|
| 133 | **Harden production-ready TrackingPage** | `src/TrackingPage.tsx`, `src/styles/layout.css` | Resolution des 2 P1 residuels de l'audit + edge cases naturels (texte long, etats vides). Score audit 17/20 -> 19-20/20 attendu. (1) **Touch targets WCAG 2.5.5** : boutons `tel:` et WhatsApp dans la card chauffeur passent de `minHeight: 40, padding: "8px 14px"` a `minHeight: 44, padding: "10px 16px"`. Boutons rating reasons (pills secondaires) passent de `minHeight: 36, padding: "6px 12px"` a `minHeight: 40, padding: "8px 14px"`. Conformite WCAG 2.1 AA Target Size sur tous les CTA. (2) **`<h1 className="sr-only">`** ajoute en debut de `<main>` vue BL : "Suivi BL XXX nom_client" — la vue client multi-dossier a deja son `<h1>` visible, la vue BL principale en manquait. Screen readers ont maintenant une hierarchie heading claire. Utility class `.sr-only` ajoutee dans `src/styles/layout.css` (pattern standard a11y). (3) **Empty state vue client multi-dossier** : si `clientDos.length === 0`, affiche une card avec `role="status"`, titre "Aucun dossier actif", sous-titre rassurant "Vos dossiers en cours apparaitront ici". (4) **Empty state vue BL** : si `tcs.length === 0`, affiche une card neutre "Aucun conteneur enregistre pour ce dossier" (au lieu d'un vide muet apres la card Informations). (5) **Truncate `vesselName + voyageNumber`** : navires CMA peuvent avoir des noms tres longs (ex: "CMA CGM JACQUES JOSEPH RIBOLI" + voyage "0BVAXN1MA"). Ajout `overflow: hidden, textOverflow: ellipsis, whiteSpace: nowrap` + attribut `title` HTML pour tooltip natif au hover (full text accessible sans casser le layout). (6) **`borderRadius: 10`** residuels (4 occurrences : btnBase rating, error card, info card, autres) -> `12` pour coherence avec `--radius-lg` du systeme. (7) **`transition: "background 0.3s"`** L171 (barre entre steps desktop) -> `"background 0.3s ease-out"` (easing explicite coherent avec autres transitions de la page, respect skill impeccable "ease-out exponential curves"). **Verifications** : 286/286 tests, build 11.88s, lint 0 erreur. **Score Nielsen attendu** : Heuristique 1 (Visibility) 3/4 -> 4/4 (empty states clairs), Heuristique 9 (Error recovery) 3/4 -> 4/4 (edge cases couverts). |

---

## FAIT — Sprint 28 : Polish design system / TrackingPage + drift fixes (impeccable)

| # | Tache | Fichiers modifies | Details |
|---|-------|-------------------|---------|
| 132 | **Polish design system Sprint impeccable** | `src/TrackingPage.tsx`, `src/Login.tsx`, `src/Setup.tsx`, `src/components/dossiers/DetView.tsx`, `src/pages/Dos.tsx`, `src/pages/Dep.tsx`, `src/styles/print.css` (CREE), `src/main.tsx` | Polish complet de TrackingPage et resolution des drifts identifies par `/impeccable audit` + `/impeccable critique` (score Nielsen 24/40 -> ~30/40). **TrackingPage.tsx** : (1) STEP_COLORS arc-en-ciel 6 hex hardcodes (#3b82f6 PORT, #8b5cf6 DISPATCH, #f59e0b TRANSIT, #ef4444 KATI, #059669 BAMAKO, #6b7280 RETOUR) remplaces par fonction `stepStateColor(done, current)` qui retourne 3 etats semantiques tokens (`var(--success)` / `var(--text-primary)` / `var(--text-muted)`) — fix critique car KATI rouge "danger universel" semait confusion chez clients maliens. (2) STEP_ICONS emojis (⚓🚚🛣️📍🏙️✅) remplaces par STEP_LETTERS mono (P/D/T/K/B/R) coherent JetBrains Mono identite Sapurai, plus 1 seul emoji ✓ pour done. (3) Card chauffeur aplatit : suppression du nesting card-in-card (sub-card `var(--success-bg)` dans `<article>`), divider semantique + grid au meme niveau (respect skill impeccable "Nested cards are always wrong"). (4) "🚚 Transport" -> "Transport" (couleur passee de `var(--success)` a `var(--text-secondary)`), "📞" emoji retire du bouton tel. (5) Header sous-ligne "Suivi de dossier" / "Suivi client" supprimee (redondant), fallback `"SAPURAI"` -> `"Suivi de dossier"` (le client doit voir son transitaire ou neutre, pas la marque vendor). (6) `<h2>"X conteneur(s)"` retire, remplace par `<section aria-label>` (le visible est evident, l'aria-label suffit pour SR). (7) CTA gradient "Envoyer mon avis" `linear-gradient(135deg, #1c1917, #292524)` -> `var(--btn-primary-bg)` + `var(--btn-primary-text)` (retire `eslint-disable`, aligne sur token systeme). (8) Border-radius unifie : 14 -> 12, 20 -> 999 (pills), 10 -> 8 selon contexte. (9) `transition: "all 0.3s"` -> proprietes explicites `background + box-shadow` (skill : "Don't animate CSS layout properties"). (10) `useEffect` print CSS injecte runtime simplifie, deplacement vers `src/styles/print.css` statique importe dans main.tsx (retire race condition crawler + CSS-in-JS hacky). Reste runtime : meta robots noindex (lie a la route /t/). (11) Import inutilise `textColorFor` retire. **Login.tsx + Setup.tsx** : btnPrimary gradient `linear-gradient(135deg, #1c1917, #292524)` remplace par `var(--btn-primary-bg)` + `var(--btn-primary-text)` (3 duplications du meme gradient resolues, tokens systeme). **DetView/Dos/Dep** : casse WhatsApp brand color uniformisee `#25d366` -> `#25D366` (3 + 2 + 1 occurrences). Le `eslint-disable` n'est pas ajoute dans les attributs JSX style inline (delicat sans casser la lisibilite) — les warnings restent informatifs (legitimes brand) sans bloquer le commit. **Verifications** : 286/286 tests, build 9.20s, lint 0 erreur. **Score Nielsen** : Heuristique Consistency 1/4 -> 3/4, Aesthetic 2/4 -> 3/4. |

---

## FAIT — Sprint 27 : Scan BL desactive (region) + Garantie conditionnelle au type TRANSIT

| # | Tache | Fichiers modifies | Details |
|---|-------|-------------------|---------|
| 131 | **Scan BL desactive (region) + UX garantie** | `src/components/dossiers/NDosForm.tsx` | (1) **Scan BL desactive** : `canScan = false && isBetaCompany(...)` car region Senegal hors free tier Gemini → quota 429 immediat. Le code reste en place, reactivation = retirer le `false &&` quand billing Google Cloud active sur le projet. (2) **Reordonnement** : "Type dossier" deplace AVANT "Garantie" dans le formulaire — UX logique car le type conditionne la presence de la garantie. (3) **Garantie conditionnelle TRANSIT** : seule la valeur `td === 'TRANSIT'` affiche le selecteur Garantie (Permanente / Louee / Vente lettre). Logique metier : importation/consommation et vehicules ne necessitent pas de lettre de garantie / caution douaniere — seuls les transits hinterland (Mali, Burkina...) traversent les frontieres. (4) **Tolerance migration** : en mode edition (`p.init`), si l'ancien dossier a `i.gr !== 'PERMANENTE'` (cas legacy ou erreur de saisie), le selecteur Garantie reste affiche pour permettre la correction. (5) **Reset auto** : quand l'utilisateur change `td` de TRANSIT vers IMPORT/VEHICULE en mode creation, `gr` revient a 'PERMANENTE' et tous les champs garantie (gar_contact, gar_tel, gar_frais, gar_caution, gar_statut) sont vides. **Tests** : 286/286 verts. Build 15.78s. Lint 0 erreur. **Effet metier attendu** : agents de transit ne voient plus le champ Garantie pour les imports classiques (gain de clarte), et impossible de saisir une garantie par erreur sur un import. |

---

## FAIT — Sprint 26 : Reactivation Scan BL (Gemini Vision) avec gate beta

| # | Tache | Fichiers modifies | Details |
|---|-------|-------------------|---------|
| 130 | **Scan BL via Gemini Vision reactive** | `src/components/dossiers/NDosForm.tsx`, `src/components/shared/AppModals.tsx` | Reactivation du module Scan BL retire au Sprint 20. **Gate beta** : `canScan = isBetaCompany(p.companyId) && !p.init && !!p.apiKey` — visible uniquement chez la compagnie test (`c_mocpodna9egt`) ET en mode creation (pas modification) ET si la cle Gemini est configuree. (1) `NDosForm` : import `ScanBL`, restauration du bandeau vert "Gagnez du temps - Scanner BL" + modal scan avec callback `onResult` qui pre-remplit BL/client/compagnie/destination/date/contact/conteneurs. State `showScan` + flag `canScan`. (2) `AppModals` : import `ScanBL` re-ajoute, restauration du modal `ml.t === "scan"` (declenche depuis menu, garde sur `cfg.geminiKey` configure), passage `apiKey={cfg.geminiKey}` et `scan={ml.scan}` au NDosForm. **Configuration cle** : la cle Gemini doit etre saisie dans Firestore directement par l'admin (companies/c_mocpodna9egt/cfg.geminiKey) ou via le panel super-admin. Le hook `useData.ts` lit `cfg.geminiKey` au niveau compagnie avec fallback sur `config/global.geminiKey`. **Tests** : 286/286 verts (le composant ScanBL existait deja, on a juste re-cable). Build 8.47s. Lint 0 erreur. |

---

## FAIT — Sprint 25 : Exploitation API CMA — TC auto-status + timeline voyage + retard ETA + batch import

| # | Tache | Fichiers modifies | Details |
|---|-------|-------------------|---------|
| 129 | **4 features CMA d'un coup (#1, #3, #4, #5)** | `src/types.ts`, `src/services/carriers.ts`, `src/hooks/useCarrierSync.ts`, `src/useData.ts`, `src/TrackingPage.tsx`, `src/ImportExcel.tsx` | Exploitation enrichie de l'API CMA-CGM Track & Trace au-dela de la simple date d'arrivee. **#1 Avancement statut TC auto** : nouvelle fonction `extractCMATcStatuses` dans carriers.ts qui detecte pour chaque TC son statut Sapurai (`PORT` si Discharged Import ACT, `DISPATCHE` si Gate Out ACT, `RETURNED` si Empty Returned ACT). `mapCarrierToPatches` retourne `tcUpdates[]` que `useCarrierSync` applique aux TC existants AVEC garde-fou anti-retrogradation via constante `TC_STATUS_ORDER` (ATTENDU=0 < PORT=1 < DISPATCHE=2 < TRANSIT=3 < KATI=4 < BAMAKO=5 < RETURNED=6) : un sync ne peut JAMAIS rétrograder un TC déjà avancé. **#3 Timeline voyage** : nouvelle fonction `extractCMATimeline` qui produit la liste ordonnee des events ARRI/DEPA avec port + UNLocationCode + date + nom navire + voyage + classifier ACT/EST + phase Export/Transhipment/Import. Stockee sur `Dossier.timeline` (max 1 entree par tuple port/type/date avec preference ACT > EST). Affichee dans la `TrackingPage` cote client via le nouveau composant `<VoyageTimeline>` (timeline verticale avec dot vert si reel, gris si previsionnel, sub mention "(prevu)"). Aussi propagee au trackDoc via `useData.shareTracking`. **#4 Detection retard ETA** : nouveau champ `Dossier.etaHistory[]` (max 5 entrees) auto-alimente quand `mapCarrierToPatches` detecte que `dos.da !== resp.arrivalDate`. Notification dediee dans useCarrierSync : "⚠ ETA reportee de Xj" (warning) ou "✓ ETA avancee de Xj" (ok). Le `etaChanged: { from, to }` est expose dans le retour de `mapCarrierToPatches`. **#5 Sync batch import Excel** : ImportExcel.tsx detecte au moment du `doImport` les BL CMA importes (regex prefixes + cp contient CMA), affiche au step 4 "Termine" un message info `"📡 N BL CMA detectes — sync auto en arriere-plan (espacement 5s, quota CMA respecte)"`. Le sync s'execute via le useEffect existant dans App.tsx qui se redeclenche sur changement `L.dos.length` (espacement 5s entre dossiers, filter `lastCarrierSync === undefined`). **Bonus** : nom du navire et n° voyage du dernier event Import exposes dans `Dossier.vesselName` et `Dossier.voyageNumber`, affiches dans la card "Informations du dossier" de TrackingPage (full-width sous la grille). **Tests** : 286/286 verts (les fixtures existantes restent compatibles, le nouveau format est opt-in via les tcStatuses/timeline si presents). Build 13.14s. Lint 0 erreur. **Effets metier attendus** : (a) les TC s'avancent tout seuls au statut PORT/RETURNED via sync auto, plus besoin de cliquer "Arrive au port" manuellement ; (b) les clients voient leur conteneur voyager Shanghai → Singapour → Dakar dans la page tracking ; (c) un decalage d'ETA est immediatement visible avec notif distinctive ; (d) un import Excel de 50 BL CMA declenche automatiquement 50 syncs en arriere-plan en ~4 minutes (espacement 5s). |

---

## FAIT — Sprint 24 : Palette landing etendue a l'app + Login retour + timeline cliquable + favicon S

| # | Tache | Fichiers modifies | Details |
|---|-------|-------------------|---------|
| 128 | **Cohesion visuelle complete** | `src/styles/theme.css`, `src/Login.tsx`, `src/Landing.tsx`, `src/pages/Dep.tsx`, `src/pages/Dos.tsx`, `src/pages/Tcs.tsx`, `src/styles/layout.css`, `public/favicon.svg`, `public/icon-192.png`, `public/icon-512.png`, `public/icon-maskable.png` | **Demande utilisateur** : aligner toute l'app sur la palette de la landing v3 + reglages UX + remplacer le favicon camion par le S Sapurai. **Realises** : (1) **theme.css** : mode light reecrit avec palette creme/taupe landing : `--bg-body: #f5f1e8` (creme), `--bg-secondary: #efe9d6` (creme +), `--bg-tertiary: #f0eada`, `--border: #d6d0c0` (taupe clair), `--text-secondary: #3d3a32` (taupe fonce, contraste 9:1), `--text-muted: #737067`, `--success: #15803d` (vert landing), `--danger: #c0392b` (rouge landing), `--warning: #a86a17` (ambre landing). Mode dark intact (preservation de la coherence dark existante). (2) **Login.tsx** : panneau gauche passe de `var(--bg-primary)` blanc a `var(--bg-body)` creme + ajout d'un prop optionnel `onBack` qui rend visible un bouton "← Retour" en haut a gauche pour revenir a la Landing. Pattern : Landing.tsx passe `onBack={() => setShowLogin(false)}` au Login monte. Si Login arrive via URL `?invite=XXX` ou `?login=1` (lien direct), pas de bouton retour (l'utilisateur passe par l'historique navigateur). (3) **Boutons exports (Option A)** : Excel et PDF passes de pleins satures (vert/rouge) a "ghost" outline (fond blanc + border creme + icone fleche coloree) dans Dep.tsx et Dos.tsx. Le `+ Nouvelle` reste noir plein (action principale). Hierarchie visuelle : action primary > exports secondaires. Boutons rouge "Oui" de confirmation suppression (Caut, Chs, Dep) gardes vifs car alertes destructives. (4) **Landing nav** : bouton `$ login` styllise en pill outline avec accent vert sur le `$`, halo vert au hover (au lieu du texte mono perdu). Lien `tracking` pointe maintenant sur l'id `#tracking` ajoute a la card FN_03 "Tracking WhatsApp" (scroll-margin-top 80 pour eviter le masquage par la nav). (5) **Timeline TC cliquable** (Tcs.tsx, panneau deplie) : le tout prochain step de la sequence est rendu cliquable avec halo vert pulsant, label souligne en pointilles verts, mention "cliquer" sous le label. Au clic : ouvre la modale de date pour transitions simples (DISPATCHE→TRANSIT→KATI→BAMAKO→RETURNED) ou le modal Dispatch pour PORT→DISPATCHE. Garde-fou : cursor not-allowed + tooltip si BAE/Pregate manquant pour passage a DISPATCHE. Animation respecte `prefers-reduced-motion`. (6) **Favicon + PWA icons** : `public/favicon.svg` reecrit avec le path SVG du logo Sapurai (S-route stroke blanc + pointilles ambre #fbbf24 sur carre arrondi noir). PNG 192/512/maskable regeneres via PIL : carre noir arrondi + grand "S" Arial Bold blanc centre. Plus de camion dans l'onglet navigateur ni sur l'app PWA installee. **Verifications** : 286/286 tests verts, build 10.6s, lint 0 erreur. Coherence visuelle complete entre landing publique et app interne. |

---

## FAIT — Sprint 23 : Fix detection events ARRI (arrivee navire) en plus de DISC

| # | Tache | Fichiers modifies | Details |
|---|-------|-------------------|---------|
| 127 | **Bug : sync CMA recuperait la mauvaise date** | `src/services/carriers.ts`, `src/services/cma.ts` | **Probleme rapporte** : BL CMA SWA0447921, ETA portail CMA = 06/05/2026 (ARRIVEE DU NAVIRE a Dakar), Sapurai recuperait 06/04/2026 (mois faux). **Cause** : le filtre `isDischarged` ne detectait que les events EQUIPMENT.DISC (decharge conteneur). Or sur le portail CMA, "ARRIVEE DU NAVIRE" est un event TRANSPORT.ARRI different (arrivee navire vs decharge TC). Le code retombait sur le fallback "any Discharged" qui pouvait remonter un event d'autre port (ex: decharge a Lekki au depart) ou rater l'ETA. **Fix** : extension de `isDischarged` (renomme conceptuellement en "isArrivalEvent") dans carriers.ts (extractCMAArrivalDate) et cma.ts (mapDCSAEvents) pour inclure : (a) `transportEventTypeCode === 'ARRI'` (DCSA TRANSPORT arrival), (b) `equipmentEventTypeCode === 'DISC'/'DCHA'` (DCSA EQUIPMENT discharge), (c) labels `internalEventLabel` contenant "arriv" (Arrival/Arrivee) ou "debarqu" (Debarquement, FR), (d) codes `internalEventCode` ARR/ARRI/DIS/DISC en plus de IDF/POD. **Garde-fou** : exclusion des labels contenant "depart" pour ne pas confondre Arrival et Departure dans le label. **Resultat** : pour SWA0447921 (Shanghai -> Dakar via Lekki), le sync remonte maintenant l'event ARRI Dakar 06/05/2026 au lieu de la date la plus recente toutes phases confondues. **Tests** : 286/286 verts (les fixtures existantes restent compatibles, le filtre est juste plus inclusif). Build 9.6s, lint 0 erreur. |

---

## FAIT — Sprint 22 : Refonte Landing page v3 (terminal/dashboard anime)

| # | Tache | Fichiers modifies | Details |
|---|-------|-------------------|---------|
| 126 | **Refonte complete Landing.tsx selon design PDF** | `src/Landing.tsx` (reecriture complete, ~640 lignes) | Refonte totale de la landing publique selon design PDF fourni par l'utilisateur. **Style** : terminal/dashboard, palette creme `#f5f1e8` + noir `#0a0a09` + accents vert/orange/ambre, typographie JetBrains Mono pour terminal blocks et Inter pour titres XL. **Sections** : (1) status bar noire ticker continu (boucle 50s, pause sur reduced-motion), (2) nav avec SapuraiLogo + version v3.0 prod + liens, (3) hero split-screen (titre 72px "Gerez vos transits sans jamais oublier une surestarie." + box terminal `$ sapurai --status` + 2 CTA + a droite mock dashboard avec urgences/stats), (4) section "le_probleme" (3 cards 200k/3-5×/30+), (5) section "la_solution" (4 fonctionnalites FN_01-04 en grille 2x2 avec terminal mockups), (6) section "ce_qui_change" (tableau 8 lignes task/legacy/sapurai), (7) section "faq" (6 questions 3x2), (8) section finale fond noir avec titre rouge glow, (9) footer creme. **Animations** : (a) ticker status bar continu, (b) typewriter terminal hero (5 lignes, 320ms/ligne), (c) compteurs animes dashboard (38/76/865k via requestAnimationFrame ease-out cubic), (d) compteurs scroll-triggered (200k/30+), (e) progress bar FN_02 0->80% au scroll, (f) Intersection Observer fade-up sur sections + cards (cascade d1/d2/d3/d4), (g) curseurs `▋` clignotants sur terminaux, (h) pulse rouge sur banner urgences, glow vert sur "active" badges, glow rouge sur titre final, (i) hover lift -3px + ombre sur cards, hover ligne tableau fond creme, (j) liens nav vert au hover. **Accessibilite** : `prefers-reduced-motion: reduce` desactive toutes les animations. **Responsive** : <900px tout en 1 colonne, ticker accelere a 35s, nav reduite. <480px status bar + nav compacte. **Hooks utilitaires** (in-file) : `useAnimatedCounter` (RAF + ease-out), `useInView` (IntersectionObserver one-shot), `useTypewriter` (delais cumulatifs setTimeout). **Build** : Landing 26.65 KB gzip 6.4 KB. **CTA voir-demo** : desactive (disabled, opacity 0.4, cursor not-allowed) en attendant video demo. **WhatsApp** : wa.me/221771234567 (placeholder a personnaliser). **Email** : sapurailogistics@gmail.com. Tests 286/286 verts, lint 9 warnings hex hardcoded sur Landing (legitimes : design tier brand permanent non-inversible en dark mode, comme le hero card DetView). |

---

## FAIT — Sprint 21 : Fix sync armateur — l'armateur fait foi sur la date d'arrivee

| # | Tache | Fichiers modifies | Details |
|---|-------|-------------------|---------|
| 125 | **Bug : sync armateur n'ecrasait pas la date deja saisie** | `src/services/carriers.ts`, `src/services/cma.ts`, `src/hooks/useCarrierSync.ts`, `src/services/__tests__/carriers.test.ts` | **Probleme rapporte par testeur** : BL CMA SWA0447921, ETA portail CMA = 06/05/2026, dossier Sapurai = 05/05/2026 (saisie manuelle). Clic "Sync armateur" -> aucune mise a jour. Cause : `mapCarrierToPatches` filtrait par `if (!dos.da)` (ne touche pas si deja set). Conception initiale : preserver la saisie manuelle. **Realite metier** : l'armateur fait foi sur la date d'arrivee (comme DPWorld pour Dakar) — la saisie agent n'est qu'un premier remplissage en attendant la donnee officielle. **Fix** : (1) `mapCarrierToPatches` (carriers.ts) : si l'API renvoie une date, on l'applique TOUJOURS. Si differente : `dosPatches.da` ecrase + log "ETA maj 05/05 → 06/05". Si identique : pas de patch (eviter pollution journal) mais `summary='ETA confirmee (06/05)'`. (2) `mapDCSAEvents` (cma.ts) : meme logique pour le format DCSA v2.2.0. (3) `useCarrierSync.syncCarrier` : pose `lastCarrierSync` meme quand aucun changement (evite de re-bruler le quota CMA 20/h pour les dossiers en sync). Quand seul `lastCarrierSync` change : `sv()` direct sans `wLog` pour ne pas creer de log SYNC_CARRIER bruyant. (4) Tests : remplacement du test "n'ecrase pas dos.da deja renseignee" (ancien comportement) par 2 nouveaux : "ecrase dos.da quand l'armateur renvoie une date differente" + "n'ecrase pas quand meme date (summary='confirmee')". 286/286 tests verts (+1). **Effet de bord intentionnel** : tous les anciens dossiers CMA actifs sans `lastCarrierSync` seront re-sync au prochain rechargement et leur `da` peut etre mis a jour si CMA a une donnee plus fraiche. |

---

## FAIT — Sprint 20 : ETA on-demand + filtre Dakar + badge source + retrait Scan BL

| # | Tache | Fichiers modifies | Details |
|---|-------|-------------------|---------|
| 124 | **4 ameliorations CMA + cleanup** | `src/components/dossiers/NDosForm.tsx`, `src/components/dossiers/DetView.tsx`, `src/services/carriers.ts`, `src/services/cma.ts`, `src/hooks/useDossierActions.ts`, `src/components/shared/AppModals.tsx`, `src/types.ts` | (1) **Bouton "📡 ETA CMA" dans NDosForm** : a cote du champ Date arrivee, bouton qui interroge l'API CMA-CGM a la demande via `fetchCarrier(bl, cp)`. Conditions : compagnie beta + cp contient "CMA" + bl >= 4 chars. Au succes : pose `da` + bascule `daSrcState='cma'` + pre-remplit la liste TC si elle est vide. Echec : toast clair. Avantage vs auto-sync : l'agent voit l'ETA *avant* de creer le dossier (pas de delay 5s magique). (2) **Filtre port destination Dakar (DKR)** : `extractCMAArrivalDate` (carriers.ts) et `mapDCSAEvents` (cma.ts) priorisent les events dont `transportCall.location.UNLocationCode === 'SNDKR'/'DKR'` ou `locationName` contient "dakar". Pool de priorite : ACT Dakar > EST Dakar > ACT Import > EST Import > tout Import > tout Discharged. Evite les rares BL multi-imports (cabotage Dakar->Banjul). (3) **Badge "📡 CMA" dans DetView** : nouveau champ `daSrc?: 'manual' \| 'cma'` sur Dossier (types.ts). Pose automatique : `mapCarrierToPatches` et `mapDCSAEvents` posent `daSrc='cma'`, NDosForm et useDossierActions (addDos, editDos, patchDos) posent `daSrc='manual'` si l'agent saisit/modifie sans flag explicite. DetView : badge "📡 CMA" gris sur le hero card a cote de la date d'arrivee (title=tooltip). (4) **Retrait du Scan BL** : pas de cle Gemini configuree pour l'instant. Suppression du bandeau vert "Gagnez du temps - Scanner BL", du bloc `<ScanBL>` inline, du modal `ml.t === "scan"`, des imports + state `showScan`, du prop `apiKey={cfg.geminiKey}`. Composant `src/ScanBL.tsx` conserve pour reactivation future. AppModals.js passe de 194 KB a 188 KB (-6 KB). **Verifications** : 285 tests verts, build 11.34s, lint 0 erreur. |

---

## FAIT — Sprint 19 : Fix timeline TC mobile (chevauchement labels/dates)

| # | Tache | Fichiers modifies | Details |
|---|-------|-------------------|---------|
| 123 | **Fix chevauchement timeline TC sur mobile** | `src/pages/Tcs.tsx` (lignes 227-236) | Probleme : sur mobile, le panneau deplie d'un conteneur affichait des labels ("Chargement"/"Sortie DK") qui se chevauchaient, et des dates collees ("20/04/202625/04/202625/04/2026"). Cause : `flex: 1, minWidth: 0` + `minWidth: 56` trop petit pour le texte "Chargement" (~80px). Le `overflowX: auto` du parent ne s'activait pas car flex compressait au lieu de deborder. **Fix** : (1) Wrapper de chaque etape : `flex: 1, minWidth: 0` -> `flexShrink: 0`. (2) Cellule label : `minWidth: 56` -> `76`. (3) Ajout `whiteSpace: "nowrap"` sur le label et la date pour empecher tout retour a la ligne dans la cellule. (4) Barre de connexion : `flex: 1, minWidth: 8` -> `width: 16, minWidth: 16` (largeur fixe au lieu de stretch). **Resultat** : desktop inchange (le scroll horizontal du parent existe deja), mobile scrolle horizontalement proprement quand les 7 etapes ne tiennent pas. **Reproduction** : commit Codespace `cbc8356` (4 mai 2026) refait en local apres abandon du push Codespace bloque par non-fast-forward sur origin/main. Build 11.51s, lint 0 erreurs. |

---

## FAIT — Sprint 18 : ETA CMA + dispatch fluide + feature flag beta + UI Accueil

| # | Tache | Fichiers modifies | Details |
|---|-------|-------------------|---------|
| 122 | **5 ameliorations UX + restriction beta** | `src/services/carriers.ts`, `src/services/cma.ts`, `src/components/conteneurs/DispForm.tsx`, `src/hooks/useConteneurActions.ts`, `src/hooks/useAppLogic.ts`, `src/App.tsx`, `src/pages/Dash.tsx`, `src/constants/featureFlags.ts` (CREE) | (1) **ETA depuis API CMA** : `extractCMAArrivalDate` (carriers.ts) et `mapDCSAEvents` (cma.ts) priorisent ACT Import > EST/PLN Import > any Import > any Discharged. Permet de remonter l'ETA quand le TC n'est pas encore arrive (events EST seuls), avec marqueur "ETA xxx (CMA)" vs "Date arrivee xxx (CMA)" dans le summary. (2) **Creation chauffeur inline dans Dispatch** : DispForm refondu avec toggle `select`/`new`. Mini-formulaire (Nom\*, Camion\*, Tel, Poids max optionnel, Tracteur) integre au modal Dispatch. Si `availChs.length === 0` : mode `new` impose direct (au lieu du warning + bouton qui fermait le modal). `useConteneurActions.dispatch` accepte un 7e param optionnel `newChData` qui ajoute le chauffeur a `chs` dans la meme transaction `sv()` (atomique, evite les races sur `db` capture en closure). (3) **Retrait blocage poids max** : suppression du filtre `if (cpm > 0 && po > 0 && po > cpm) return false;` dans `isAvailable`. Le recap rouge "⚠ SURCHARGE (dispatch possible mais a vos risques)" affiche l'alerte mais n'empeche plus le dispatch. Recap aussi affiche en mode `new` (a partir des champs saisis) et meme sans TC actifs (cas chauffeur seul + nouveau TC en surcharge). (4) **Feature flag beta** : nouveau `src/constants/featureFlags.ts` avec `BETA_COMPANIES = ['c_mocpodna9egt']` et helper `isBetaCompany(companyId)`. `App.tsx` : auto-sync CMA dans `useEffect` desactive si non-beta + prop `syncCarrier` passee a `undefined` aux non-beta (cache le bouton "Sync armateur" dans le menu DetView). Permet de stabiliser CMA chez le fondateur avant ouverture generale. (5) **Banner urgences redondant retire** : suppression du `<div>` "⚠️ X critique(s) en cours" dans Dash.tsx (Actions rapides) qui doublonnait avec le badge "X urgence(s)" en haut a droite. Variables `critCount`, `sysStatus`, `sysBg` retirees de la destructuration props. **Verifications** : 285 tests verts, build OK 15s, lint 0 erreur. |

---

## FAIT — Sprint 17 : Refonte UI handoff design (tokens + polices + logo + structure)

| # | Tache | Fichiers modifies | Details |
|---|-------|-------------------|---------|
| 121 | **Refonte UI Sprint 1+2 fondations** | `index.html`, `src/styles/theme.css`, `src/styles/layout.css`, `src/components/ui/SapuraiLogo.tsx` (CREE), `src/components/layout/Sidebar.tsx`, `src/components/layout/TopBar.tsx`, `src/Login.tsx`, `src/Setup.tsx`, `src/App.tsx`, `src/pages/Dash.tsx`, `src/pages/Caut.tsx`, `src/components/dossiers/DetView.tsx` + bulk `src/**` | Refonte design issue du handoff `sapurai.zip` (16 maquettes JSX + README 370 lignes). **Sprint 1 — Fondations** : (1) Polices Inter (UI) + JetBrains Mono (BL/TC/montants/identifiants) chargees via Google Fonts avec preconnect, body fontFamily=Inter. (2) Design tokens `theme.css` refondu : palette light bg=#f7f7f5 / surface=#ffffff / text=#0a0a09 / alert=#9a3412 (orange foncé) / warning=#854d0e (jaune-brun) / success=#166534 / info=#1e40af / btn-primary-bg=#0a0a09 ; palette dark bg=#0a0a0a / surface=#161615 / alert=#fb923c / warning=#facc15 / btn-primary-bg=#fafaf9 (s'inverse). Ajout vars `--font-sans` + `--font-mono` + `--radius-sm/--radius/--radius-lg`. (3) Composant `SapuraiLogo` (Logo A handoff : carre arrondi noir, "S" trace comme route avec pointilles ambre #fbbf24) avec props size/color/showText/style. **Sprint 2 — Shell + Login** : (4) Sidebar 220px → **248px** (handoff). Header tenant : fontSize 17 fontWeight 700 letterSpacing -0.015em (vs 800/+0.3 avant). Sous-titre 12px (vs 11px). (5) Items nav refondus : padding 10px 12px, icone 20px ; **actif = fond btn-primary-bg + texte btn-primary-text** (s'inverse en dark) au lieu de fond gris bg-hover. Badge dossiers count → mono compact tabular-nums. Signature "Propulsé par <SapuraiLogo>". (6) Login **split-screen ≥1024px** : formulaire à gauche, panneau noir Sapurai à droite (logo, tagline "Tout votre transit. Au même endroit.", 3 stats Sync DPWorld / WhatsApp tracking / Hors ligne PWA), motif diagonale fines opacity 0.08. Mobile : layout centre conservé. (7) Topbar recherche : width 320 → 480px, padding genereux, fontFamily Inter. **Sprint 3+4 — KPIs + identifiants** : (8) KPIs Dash + Caut : fontSize 20→28, fontWeight 800→600, fontFamily mono, tabular-nums, letterSpacing -0.02em (style handoff). (9) Bulk replace `fontFamily: "monospace"` → `var(--font-mono)` sur 16 fichiers via sed. (10) Bulk replace `fontFamily: "system-ui, sans-serif"` → `var(--font-sans)` sur l'ensemble du code. **Sprint 6 — Espace client** : (11) Header client refondu avec `<SapuraiLogo>` + nom tenant. **Sprint 7 — Setup** : (12) Setup : Inter + logo SapuraiLogo. **Sprint 10 — Dark mode audit** : 3 hex hardcoded restants acceptables (semantiques stColors). **Sprint 11 — Pages secondaires** : Caut KPIs alignes au style handoff. **Verifications** : tests 255/255, build 11.29s, lint OK. **Sprint 5 (workflow validation 4 etapes Agent→Visa terrain→Admin→Paiement)** + **Sprint 8 (mobile React Native dedie)** = sprints futurs (architecture nouvelle requise, demandent decisions produit). **Resultat utilisateur** : interface alignee identite Sapurai (logo S-route + ambre), typo Inter moderne, identifiants en JetBrains Mono lisibles, palette neutre/orange/jaune coherente light/dark. Refonte = pure cosmetique + structure : zero impact fonctionnel, zero regression. |

---

## FAIT — Sprint 16 : Retours testeur — Bugs, workflow auto, assignations, depenses, journal

| # | Tache | Fichiers modifies | Details |
|---|-------|-------------------|---------|
| 120 | **Sprint A — 9 bugs/items debloquants** | `src/useData.ts`, `src/components/dossiers/NDosForm.tsx`, `src/hooks/useConteneurActions.ts`, `src/hooks/useAppLogic.ts`, `src/App.tsx`, `src/components/shared/AppModals.tsx`, `src/components/dossiers/DetView.tsx`, `src/pages/Stats.tsx`, `src/hooks/useDossierActions.ts`, `src/pages/Dash.tsx` | (1) Bug "Marquer fait" : `sanitize()` recursif des `undefined` avant `setDoc` + message d'erreur revele code Firestore precis (permission-denied, etc.). (2) Assignation agent a la creation : sélecteur dropdown + checkboxes taches + toggle voirDepenses + ecriture dans `dos.itv` au save. (3) Suppression TC : nouvelle fn `deleteTc` + bouton dans DetView (visible si TC pas en transit/retourne) + suppression cascade des Depenses liees. (4) TC importes Excel non editables : `initTcs` filtre etendu aux TC `ATTENDU` (etaient exclus). (5) Filtre Stats Annee : graphe d'evolution suit le filtre `per` (year=12 mois annee courante, quarter=3 mois trimestre, month=1 mois, all=6 derniers). (6) Rappels factures obsoletes : recalage `dt` des stubs auto en_attente_facture mt=0 quand `da` change (editDos + patchDos). (7) Bouton Modifier : pattern `prev` ajoute pour retour a la modale precedente (DetView). (8) Carte Factures impayees Accueil (couleur warning, pattern Cautions). (9) Compteur dossiers archives Accueil. |
| 119 | **Sprint B — 3 items workflow auto** | `src/utils/dossierStatus.ts` (CREE), `src/utils/__tests__/dossierStatus.test.ts` (CREE), `src/hooks/useConteneurActions.ts`, `src/hooks/useAppMetrics.ts`, `src/components/shared/DetentionModal.tsx` (CREE), `src/components/shared/AppModals.tsx` | (1) Statut dossier auto : helper `computeDossierStatus(d, tcs)` + `applyAutoStatus(dosList, tcsList)` (regles : tous ATTENDU=>INITIALISE, ≥1 PORT=>SECURISE, tous transit=>EN_TRANSIT, jamais ecrasement de CLOTURE/ARCHIVE). Appele dans `dispatch` et `advance`. 8 tests unitaires. (2) Cloture conditionnelle detention : `advance()` detecte les jours detention au passage RETURNED. Si > franchise retour vide => bloque cloture, ouvre `<DetentionModal>` avec 3 voies (banque / defalcation caution / plus tard stub). Validation = cloture le dossier + cree Depense detention_vide. (3) Alerte arrivee imminente : nouvelle categorie `ARRIVEE_IMM` dans urgences, affiche dossiers avec `da` a J-0 a J-5 ayant ≥1 TC en ATTENDU. Niveau critical a J-0/J-1, warning au-dela. |
| 118 | **Sprint C — 2 items assignations agents** | `src/TeamPanel.tsx`, `src/components/dossiers/NDosForm.tsx`, `src/useData.ts`, `src/main.tsx`, `src/App.tsx`, `src/pages/AgentView.tsx` | (1) Exclusivite : un dossier = max 1 agent. Filtres dans selecteur creation invite (exclut dossiers ayant un AGENT itv), modale edition (autorise uniquement dossiers sans agent OU dont agent === editingMember), NDosForm avec warning visuel si remplacement. Garde backend dans `updateMemberAssignments` qui leve une erreur listant les conflits. (2) Vue agent etendue : nouveau bloc "Actions rapides" dans cards AgentView (cycle BAD/BAE 3-etats, saisie pregate inline, advance TC PORT->DISPATCHE->...->RETURNED, upload doc). Notif admin systematique via nouvelle fn `notifyAdmins` dans useData (pose notif `for: 'ADMIN'` consommee par tous les admins via listener elargi). |
| 117 | **Sprint D — 3 items depenses + cautions + listes ref** | `src/types.ts`, `src/components/dossiers/NDosForm.tsx`, `src/hooks/useConteneurActions.ts`, `src/hooks/useAppLogic.ts`, `src/App.tsx`, `src/pages/Caut.tsx`, `src/hooks/useDossierActions.ts` | (1) Listes ref : `<datalist>` HTML5 sur Compagnie / Destination / Contact garant dans NDosForm. Listes derivees automatiquement de l'historique des dossiers (pas de catalogue separe). (2) Caution unitaire vs total + suivi TC par TC : champs `gar_caution_unit` + `gar_frais_unit` sur Dossier ; `gar_recup` + `gar_recup_dt` + `gar_recup_note` sur Conteneur. NDosForm : inputs unitaires avec total auto-calcule live (montant × nb TC). Nouvelle fn `patchTc` exposee. (3) Cautions auto dans Depenses : `updateGarantie` detecte passage a VERSEE et cree automatiquement Depense `categorie: caution` (montant gar_caution) + Depense `categorie: lettre_garantie` (montant gar_frais). Anti-doublon. Statut payee. |
| 116 | **Sprint E — Module Journal repositionne** | `src/components/layout/Sidebar.tsx`, `src/components/shared/AppModals.tsx`, `src/components/dossiers/DetView.tsx` | Rename "Journal" -> "Historique des actions" dans sidebar et titre modale. Ajout acces contextuel depuis DetView (bouton "Historique du dossier" dans le menu, entre "Joindre documents" et "Sync DPWorld"). Logs filtres automatiquement sur le dossier ouvert (ml.did) + retour DetView via pattern `prev`. |
| 115 | **Total session : 18 items livres** | — | Sprint A (9) + B (3) + C (2) + D (3) + E (1). Tests : 246/246 -> 255/255 (+9). Build + lint + deploy verts a chaque fin de sprint. Aucune regression detectee. Backlog testeur entierement traite en une session. |

---

## FAIT — Sprint 15 : Securite (CI, tracking tokens, auth Google, plan migration)

| # | Tache | Fichiers modifies | Details |
|---|-------|-------------------|---------|
| 81 | CI GitHub Actions | `.github/workflows/ci.yml` (CREE) | Workflow lint + build + test sur push/PR. Filet automatique avant deploy. Node 20, cache npm. |
| 82 | Auth Google Sign-In | `src/firebase.ts`, `src/Login.tsx` | GoogleAuthProvider expose. Bouton "Continuer avec Google" sur ecrans home/login/register/invite. Flux invite : stocke pending join avant popup. Conserve auth email en parallele. |
| 83 | Token aleatoire tracking public | `src/types.ts`, `src/hooks/useAppLogic.ts`, `src/useData.ts`, `src/main.tsx`, `src/TrackingPage.tsx`, `firestore.rules` | URL `/t/{companyId}/{bl}` previsible remplacee par `/t/{uuid}` imprevisible. `tokId` genere via `crypto.randomUUID()` a la creation du dossier (addDos) ou au premier partage (retrocompat dossiers existants). `clientTokens` dans cfg pour tracking client. trackDoc contient `companyId` et regle Firestore verifie `request.resource.data.companyId` au lieu du regex sur ID. Persistance via `setDoc merge:true` direct pour eviter race avec `save()` fire-and-forget. |
| 84 | Plan migration subcollections Firestore | `MIGRATION-SUBCOLLECTIONS.md` (CREE) | Document de 11 sections : contexte, seuils de declenchement, schema actuel/cible, strategie dual-write progressif, prerequis, risques, metriques, 4 phases detaillees (A dual-write → B backfill → C switch reads → D cleanup), rollback par phase, estimation 6-7 jours dev / 3-4 semaines calendaires. A executer quand doc company > 500 KiB ou > 10 clients actifs. |
| 85 | Securite tracking public : noindex + OG + WhatsApp | `index.html`, `public/robots.txt` (CREE), `public/og-tracking.png` (CREE), `src/TrackingPage.tsx` | `robots.txt` Disallow /t/*, meta `noindex,nofollow,noarchive` injectee dynamiquement pour crawlers ignorant robots.txt. Open Graph + Twitter Card dans index.html avec image 1200x630 generee via canvas (html2canvas). Bouton WhatsApp (vert #25D366) a cote du tel pour chauffeurs +221 ou +223, message pre-rempli citant le numero TC. |
| 86 | Refactor TrackingPage : composant partage | `src/TrackingPage.tsx` | Extraction `<TcTrackingCard>` et `<Footer>` du block duplique (~100 lignes) entre vue BL et vue client. Prepare les polish stepper + a11y sans double travail. Remplacement de 3 couleurs hardcoded par CSS variables pour prefers-color-scheme. |
| 87 | Polish TrackingPage : skeleton, a11y, stepper, share, print | `src/TrackingPage.tsx` | Fondations : error state avec bloc "Que faire ?", loading skeleton au lieu du spinner emoji, a11y (landmarks header/main/section/article, ol/li aria-current="step", aria-label statut/boutons, aria-busy). Layout : stepper vertical sous 480px (hook `useIsNarrow` via matchMedia). Details : timestamp relatif ("il y a 2h") + tooltip absolu, bouton Partager avec navigator.share + fallback clipboard, print styles via `@media print` injectes sur document.head (masque footer/bouton, page-break-inside pour TC cards). |
| 88 | Impeccable TrackingPage : typo, spacing, contrastes | `src/TrackingPage.tsx` | Typography scale consolidee (11/12/13/14/15/18, suppression des 9 et 10px sous le seuil de lisibilite). Spacing rhythm sur 4/8/12/16/20. Contrastes WCAG AA : remplacement `text-muted` (#a8a29e, 2.8:1) par `text-secondary` (#78716c, 5.3:1) pour tout texte sous 14px sur bg-primary. Error "Que faire ?" converti en `<ul>/<li>` semantique. |
| 89 | Types domaine Dossier strict | `src/types.ts`, `src/hooks/useAppLogic.ts` | Retrait de l'escape hatch `[key: string]: unknown` sur Dossier (typage devient vrai filet compilateur). 6 champs ajoutes (bs, nd, rv, pf, cr, ce) + 3 draft-only (tcs, nbTc, defType). Intervenant complete : id, role, tachesDone, renommage tasks→taches et tel→tl. useAppLogic : `dos: any[]` → `dos: Dossier[]`. Baseline tsc --noEmit : 146 erreurs pre-existantes, +0 apres typage. Prochaines sessions : Conteneur, Depense, Chauffeur. |
| 90 | Phase 1 dark mode : CSS vars WCAG AA | `src/styles/theme.css` | Audit a revele 4 paires en echec AA. Retouche des variables dark : `--text-secondary` 4.08→6.5:1, `--text-tertiary` remonte, `--text-muted` 2.30→3.2:1 (decoratif), `--success` 4.53→5.5:1, `--purple` 2.89→5:1, `--purple-text` 4.37→6.3:1. Light mode intact. |
| 91 | Phase 2 dark mode : DetView | `src/components/dossiers/DetView.tsx` | Fix des hardcodes dans DetView. Hero card : `var(--btn-primary-bg)` s'inversait (light en dark → texte blanc invisible), remplace par gradient hardcode `#1c1917→#292524`. Phrase humaine (`#1e3a5f` navy) → `var(--info-bg)`/`var(--info-text)`. Franchises surestaries (`#1e1b4b`) → `var(--danger-text)`. Dots etape (`#d6d3d1`) → `var(--border)`. Textes BAD/BAE/PREGATE sur danger-light : `--text-muted` → `--text-tertiary`, `--danger` → `--danger-text`. |
| 92 | STYLE-GUIDE.md | `STYLE-GUIDE.md` (CREE) | Regles design + dark : interdiction hex en dur (exceptions), hierarchie tokens texte avec ratios AA, pairs `--X-bg`/`--X-text`, typo scale 11/12/13/14/15/18, spacing 4/8/12/16/20/32, checklist merge, helper JS console pour calculer contraste, historique corrections. |
| 93 | Types domaine Conteneur strict | `src/types.ts`, `src/hooks/useAppLogic.ts` | Audit : 6 champs manquants ajoutes (ch, cm, tl, dtk, dak, dab, budget, inc). Escape hatch retire. useAppLogic : `tcs: any[]` → `tcs: Conteneur[]`. +0 erreur tsc. |
| 94 | Types domaine Depense strict | `src/types.ts`, `src/hooks/useAppLogic.ts` | Bug revele : l'interface avait un champ `st` **jamais** lu par le code (qui utilise `s`). Masque par l'escape hatch. Correction : `s?`, `ht?`, `nf?`, `ds?`, `ph?`, `tcid?`, `fi?` ajoutes. `st` et `ob` supprimes. Escape hatch retire. useAppLogic : `dep: any[]` → `dep: Depense[]`. +0 erreur tsc. |
| 95 | Types Chauffeur + dark main.tsx | `src/types.ts`, `src/hooks/useAppLogic.ts`, `src/main.tsx` | Interface Chauffeur reecrite : 4 champs morts supprimes (tel, veh, imm, missions), 7 champs reels ajoutes (cm, tl, tr, pm, tty, bl, blr). Escape hatch retire. +0 erreur tsc. main.tsx : 34 hex hardcoded sur 5 ecrans root (error boundary, splash, compte suspendu, switch company, SPLASH const) remplaces par CSS vars. Dark mode coherent sur les ecrans systeme. |
| 96 | Dark mode TeamPanel + ScanBL | `src/TeamPanel.tsx`, `src/ScanBL.tsx` | Derniers fichiers avec hex hardcoded. TeamPanel (27 hex) : body, cards membres, tabs, inputs, selects, boutons, ROLE_COLORS, dots online/offline. ScanBL (21 hex) : preview cards, labels, loading, erreur, boutons primaire/ghost. Chantier dark mode termine : aucun hex hardcode restant hors exceptions STYLE-GUIDE (WhatsApp brand, STEP_COLORS semantiques). |
| 97 | F.1 Tests useAppLogic (filet avant refactor E) | `package.json`, `vitest.config.ts` (CREE), `src/hooks/__tests__/useAppLogic.test.ts` (CREE) | Setup @testing-library/react + happy-dom pour tester un hook React. 33 tests ajoutes : smoke (1), toggleDepSt (5), patchDos (4), closeDos (4), addDos (8), dispatch (11). Couvre happy path + edge cases principaux (BL doublon case-insensitive, dedup TCs, blocage sans BAE/pn, promotion statut dossier). 2 TODO "verifier si comportement correct" laisses : side-effect modale pregate sur DPWORLD paye, non-cascade TCs lors de closeDos manuel vs auto-cloture dans advance(). 108 → 141 tests. Install via --legacy-peer-deps (conflit preexistant ESLint 10). |
| 98 | Design critique Login (3 fixes) | `src/Login.tsx` | Application d'une critique design structuree. (1) Hierarchie CTA home : Google primary (white + shadow brand) / Email secondary ghost / Invitation tertiary link, au lieu de 3 boutons egaux. (2) Desktop : bullets produits (Temps reel / Multi-equipes / Hors ligne) sous la card, visible >= 768px. (3) btnPrimary utilise gradient dark permanent au lieu de `var(--btn-primary-bg)` qui s'inversait en dark (meme pattern que hero DetView). Bonus : placeholder email generique, separateurs "ou" uniformises avec lignes, inputs avec background/color CSS vars pour dark coherent. |
| 99 | Audit design 12 pages + 3 fixes critiques | `AUDIT-DESIGN.md` (CREE), `src/pages/AgentView.tsx`, `src/pages/Dash.tsx`, `src/pages/Stats.tsx`, `src/styles/theme.css`, +5 composants | Audit structure des 12 pages (40+ findings). 3 fixes critiques appliques : (1) AgentView `"SAPURAI"` hardcode → prop `companyName` (pattern multi-tenant). (2) `--text-muted` (ratio 3.2:1 echec WCAG AA) remplace par `--text-secondary` sur texte < 14px dans Dash (19 occurrences), DetView, IntervenantsView, JdocView, NDosForm, Stats. Labels HT/TAXES passent de 9→10px. Empty states decoratifs conservent muted. (3) Palettes hardcoded (PAL 10, typeColors 11, stColors 5, ST_BG 6, tyColors2 6) remplacees par CSS vars theme-aware. Ajout `--chart-1` a `--chart-10` (light + dark) dans theme.css. AgentView `ST_BG` → `--sc-*` tokens existants. |
| 100 | ESLint rule no-hex-in-style + enforcement STYLE-GUIDE | `eslint.config.js`, `STYLE-GUIDE.md`, `src/components/dossiers/DetView.tsx`, `src/TrackingPage.tsx` | Sans rule auto, STYLE-GUIDE violable silencieusement (revele par audit : 88+ hex hardcoded subsistaient). Ajout de `no-restricted-syntax` avec selecteur AST `JSXAttribute[name.name='style'] Literal[value=/#.../]` + variante template literal. Warning avec message pointant vers le guide. Bypass legitime via `// eslint-disable-next-line no-restricted-syntax -- <raison>`. 4 exceptions annotees (hero card dark permanent DetView, 3 WhatsApp brand color). STYLE-GUIDE section 0 documente enforcement + pattern bypass + backlog 21 warnings a purger. |
| 101 | **Refactor E : split useAppLogic God-hook** | `src/hooks/` (8 fichiers CREES), `src/hooks/useAppLogic.ts` (REWRITE orchestrator), `src/hooks/__tests__/useAppLogic.test.ts`, `src/App.tsx` | Le hook de 639 lignes a ete decoupe en 8 hooks specialises : useToast (22), useAppMetrics (150), useDossierActions (115), useConteneurActions (130), useChauffeurActions (35), useDepenseActions (85), useDPWorldSync (90), useImportActions (105). useAppLogic devient l'orchestrator (140 lignes) qui compose et expose une API unique. **Pattern return-value applique sur toggleDepSt** (TODO #1 decide Option A) : ne declenche plus `setMl` en interne, retourne `{ ok, needsPregate? }` que App.tsx wrap. Tests F.1 mis a jour (5 tests toggleDepSt sur 33) pour la nouvelle signature. Tous verts (141). API publique preservee pour App.tsx consumers, zero regression. TODO #2 (cascade closeDos) reste bloque sur input Ibrahima mais n'empeche pas le split. |
| 102 | **Doc : refonte PROJET.md + bump v4.0.0** | `PROJET.md` (REWRITE), `package.json`, `CHANGELOG-AUDIT.md` | PROJET.md etait obsolete : titre "LogiTrans", URL `logitrans-integrated-services.web.app`, version 3.2.0, structure fichiers en `.jsx`/`.js`, hooks listes comme 1 fichier (useAppLogic). Refonte complete alignee sur l'etat reel : (1) Header → Sapurai, URL `sapurai-84984.web.app`, version 4.0.0 (bump major : rename + migration TS = breaking), date 2026-04-19. (2) Vue d'ensemble → "transitaires Dakar (intérieur Sénégal + hinterland Mali/Burkina)" coherent avec index.html. (3) Stack → ajout TypeScript 5.9, Vitest, Cloudflare Worker. Deps → ajout jspdf, qrcode.react. (4) Structure fichiers → tous `.tsx`/`.ts`, ajout types.ts/services/dpworld.ts/Landing.tsx/AdminPanel.tsx/fileStore.ts, hooks detailles (12 fichiers + __tests__), components/ui/, utils elargi (pdf, export, importHelpers, validate, contrast). (5) Firestore → ajout collection `/tracking/{tokId}`, mention mono-doc + plan migration. (6) Roles → ajout super-admin. (7) Fonctionnalites → sections Sync DPWorld, Dark mode, Admin multi-tenant, QR tracking. (8) Tests → section dediee F.1. (9) Deploiement → IDs Firebase corrects + headers securite. (10) Changelog → entree v4.0.0 synthetisant Sprint 13-15. package.json bump 3.0.0 → 4.0.0. |
| 104 | **Rating client sur tracking page + remontee Dash/DetView** | `src/types.ts`, `firestore.rules`, `src/TrackingPage.tsx`, `src/useData.ts`, `src/pages/Dash.tsx`, `src/components/dossiers/DetView.tsx`, `src/utils/rating.ts` (CREE), `src/utils/__tests__/rating.test.ts` (CREE), `PROJET.md` | Feedback client a la cloture du dossier. **Cote client** (tracking page publique, sans auth) : widget "Votre avis" visible si `dosSt ∈ {CLOTURE, ARCHIVE}` — 3 niveaux (1=Tres satisfait / 2=Correct / 3=Probleme) avec emoji. Si rating=3 : checklist 5 cases (Retard/Communication/Tarif/Qualite/Autre) + textarea 200 car max. Submit via `updateDoc` + `serverTimestamp()`. Irreversible V1. Marqueur auteur via `localStorage('sapurai-rated-${tokId}')` — approximation UX (non-auth). **Securite** : rules Firestore `allow update` public strict — `resource.data.rating == null`, `hasOnly(['rating','ratingComment','ratingReasons','ratingAt'])`, `rating in [1,2,3]`, `ratingAt == request.time` (force serverTimestamp), comment ≤ 200 car, reasons ≤ 5. En OR avec `allow write` auth proprietaire. UUID v4 (2^122) protege brute force. shareTracking/syncTracking passent en `{merge: true}` + ajout `dosSt` pour ne pas ecraser le rating ecrit par le client. **Cote patron** : listener collection `/tracking where companyId == cid` dans useData, build index tokId→rating, merge sur `dos` au retour. Pas de Cloud Function. Dash : KPI "Satisfaction client" (% rating 1+2) + bandeau rouge "Problemes signales" avec liste triee (recent → ancien), clic → ouvre dossier. DetView : badge "Avis client" dans hero card + bouton WhatsApp "Rappeler le client" si rating=3 et tel +221/+223. **Tests** : 7 unitaires sur `utils/rating.ts` (ratingStats, dossiersWithProblems, edge cases vides / 100% / 0% / tri / sans date). Total 141 → 148. Build + tsc + deploy OK. **Decisions produit** : 3 niveaux avec labels honnetes ("Correct, pas parfait" donne permission etre neutre, limite biais CSAT), inline non-intrusif (pas popup), checklist+texte uniquement rating=3, rating visible pour auteur (localStorage), invisible autres visiteurs. |
| 119 | **Refonte AgentView : cards dossiers + statut taches + permission Depenses** | `src/App.tsx`, `src/pages/AgentView.tsx` (REWRITE) | UX agent revolutionnee : remplacement de l'ancien layout boutons toggle par des **cards riches par dossier**. Header personnalise avec nom + taches habituelles (union des taches sur tous ses dossiers). Pour chaque dossier : info client/BL/compagnie + TCs avec statuts theme-aware + section "Mes taches" detaillee. Chaque tache affiche son **statut reel sur le dossier** (BAD: Obtenu/En cours/A demander, BAE: idem, PREGATE: N° ou A obtenir) avec badge couleur + bouton "Marquer fait/Termine" inline. Section "Depenses" **conditionnelle** au flag `intervenant.voirDepenses` — si true affiche liste 5 dernieres depenses + total paye/TTC, sinon section masquee. Dependance `dep` ajoutee aux props (passe depuis App.tsx). Cas vide reformule : "En attente d'assignation \u2014 votre responsable vous assignera des dossiers depuis Parametres -> Equipe -> Gerer assignations" (au lieu du texte ancien qui supposait deja une procedure manuelle). Build 12.04s, lint 0 errors, 246/246 tests. |
| 118 | **Edition assignations agent existant (modale Gerer assignations)** | `src/useData.ts`, `src/main.tsx`, `src/components/shared/SettingsForm.tsx`, `src/TeamPanel.tsx` | Permet a l'admin d'ajouter/retirer des dossiers + taches + voirDepenses **sur un agent deja membre**, sans recreer une invite. **useData.updateMemberAssignments(memberName, assignments[])** : lit le doc company, parcourt dos[], pour chaque dossier ajoute/met a jour/retire l'agent dans `dos.itv` selon presence dans assignments. Persistance setDoc merge en une seule transaction. Preserve `tachesDone` existant et l'id Intervenant pour stabilite. **TeamPanel** : nouveau bouton "Gerer assignations" (info-bg) sur chaque ligne membre role=agent. Modale plein ecran avec meme UX que la creation d'invite (chips dossiers + per-dossier taches + voirDepenses + recherche pour ajouter). Initialisation : lit dos[].itv pour reconstruire les assignments existants de l'agent. Bouton "Annuler/Enregistrer" + spinner saving. Propagation prop `updateMemberAssignments` via teamProps depuis main.tsx. Build OK, 246/246 tests. |
| 117 | **Creation invite enrichie : assignations dossiers + taches + voirDepenses** | `src/types.ts`, `src/useData.ts`, `src/main.tsx`, `src/components/shared/SettingsForm.tsx`, `src/TeamPanel.tsx` | Refonte du modele d'invite pour pre-assigner dossiers + permissions a la creation, evitant que l'agent atterrisse sur un ecran vide. **types.Intervenant** : ajout `voirDepenses: boolean` (permission explicite, separee des taches — un agent meme avec taches ne voit pas les depenses sans cette permission). **createInvite signature** : `(role, email, assignments[], responsabilites)` ou `assignments = [{dosId, taches, voirDepenses}]`. **joinWithCode** : applique les assignments en lisant le doc company et ajoutant l'agent comme `Intervenant` sur chaque dossier (taches + voirDepenses pre-remplis). Si l'agent est deja intervenant (edge case), met a jour ses droits. Transaction setDoc merge. **TeamPanel UI** : tab "Code invitation" enrichi avec selecteur recherche dossiers (chips style) — input recherche par client/BL/compagnie (>= 2 caracteres), liste deroulante max 10 resultats, dossiers actifs uniquement. Pour chaque dossier coche : checkboxes 7 taches (BAD/BAE/PREGATE/TRANSIT/LIVRAISON/MANUT/FACT) + checkbox "Voir les depenses". Filtre exclut CLOTURE/ARCHIVE et les dossiers deja assignes. Build 17.32s, lint 0 errors, 246/246 tests. |
| 116 | **Rapport de synchronisation DPWorld (modale detaillee) + retrait dark toggle sidebar** | `src/components/layout/Sidebar.tsx`, `src/services/dpworld.ts`, `src/hooks/useDPWorldSync.ts`, `src/hooks/useAppLogic.ts`, `src/components/shared/SyncReport.tsx` (CREE), `src/components/shared/AppModals.tsx`, `src/hooks/__tests__/useDPWorldSync.test.ts` | Apres syncAllDPWorld l'utilisateur avait juste un toast "3 dossiers synchronises" sans savoir lesquels avaient bouge. Nouvelle UX : **modale detaillee auto-ouverte** avec liste cliquable des dossiers modifies. **services/dpworld.ts** : `mapDPWorldToPatches` retourne maintenant aussi `changes: string[]` (tableau des libelles humains deja construit en interne : "Date arrivee 10/04", "BAD → Obtenu", etc.). **useDPWorldSync** : accumulate `changesByDos` et `stubCountByDos` pendant la boucle batch, construit un `SyncReport` structure `{items: SyncReportItem[], totalChanges, totalStubs, errorsBL[]}`, trie par volume de changements decroissant, puis `setMl({t: "syncreport", report})` pour ouvrir la modale automatiquement (pattern deja utilise ailleurs dans le hook). **SyncReport.tsx** (nouveau) : header stats 4 cartes (dossiers maj / changements / factures en attente / BL non trouves), liste cliquable ClickableDiv par dossier avec `<ul>` des changes + badge warning si stubs crees, zone danger pour BL non trouves, bouton Fermer. Clic sur un dossier ouvre direct son detail (`setMl({t: "det", did})`). **AppModals** : import + handler pour `ml.t === "syncreport"`, Overlay w=720. **Sidebar** : retrait du toggle dark mode (ligne 70) — desormais repetition inutile avec Parametres > Preferences. Commentaire explicatif laisse en place. **Tests** : +2 (`ouvre la modale syncreport apres syncAllDPWorld avec changements` + `n ouvre PAS la modale si aucun changement`). 244 → 246 tests verts. Build 11.93s, lint 0 errors. |
| 115 | **Refonte SettingsForm en 2 tabs (Preferences + Equipe) + retrait franchises/API keys** | `src/components/shared/SettingsForm.tsx` (REWRITE), `src/components/shared/AppModals.tsx`, `src/App.tsx`, `src/main.tsx`, `src/TeamPanel.tsx` | Panel Parametres refondu en hub de configuration. **Preferences** : uniquement toggle dark mode avec bouton visuel (emoji soleil/lune + label dynamique "Mode clair/sombre"). Retrait des champs Franchise Port/Retour/Tarif surestaries (redondant avec parametres metier dossier depuis commit 2 de l'auto-stub) et des 4 cles API armateurs (Maersk/CMA/Hapag/MSC — anti-pattern SaaS : les cles sont desormais gerees globalement cote proxy Cloudflare Worker, aucune config client requise). Note UX dans le panel : "Les franchises sont desormais configurables directement sur chaque dossier". **Equipe** (admin uniquement) : integre `<TeamPanel embedded>` via Suspense lazy. TeamPanel supporte le nouveau prop `embedded: boolean` — si true, masque le header fullscreen SAPURAI + bouton Retour. **Prop chain** : `main.tsx` bundle `{members, currentUid, createInvite, addMemberByEmail, updateMemberRole, removeMember}` dans un seul `teamProps`, transmis via App.tsx → AppModals → SettingsForm → TeamPanel (evite pollution API). Overlay elargi w=500 => w=720 pour contenir le contenu equipe. Build 12.15s, lint 0 errors, 244/244 tests. |
| 114 | **Invitations enrichies : email obligatoire + taches + responsabilites** | `src/useData.ts`, `src/TeamPanel.tsx`, `firestore.rules` | Refonte du flow invitation pour securite + onboarding riche. **createInvite** : signature passe de `(role)` a `(role, assignedEmail, taches?, responsabilites?)`. Email **obligatoire** (validation regex), normalise en lowercase. Taches: string[] optionnel (ex: ['BAD', 'BAE']). Responsabilites: string libre max 300 car. Stocke dans `/invites/{code}` comme assignedEmail, taches, responsabilites. **joinWithCode** : (1) verifie assignedEmail vs email du nouveau compte — si mismatch, throw "Ce code d'invitation est reserve a X@Y.com" ; (2) pre-remplit taches et responsabilites sur le doc membre a la creation. **firestore.rules** : invites/{code} create impose `assignedEmail is string` (contrainte validation). **TeamPanel UI** : tab "Code invitation" enrichi — input email obligatoire (avec aide "Seule cette adresse pourra utiliser le code"), checkboxes taches (7 valeurs BAD/BAE/PREGATE/TRANSIT/LIVRAISON/MANUT/FACT visibles uniquement si role=agent, style toggle button info/bg-secondary), textarea responsabilites (300 car max avec compteur). Affichage liste membres : badges taches (info-bg) + ligne italique responsabilites sous le nom/role. **useData listener** : stocke sessionStorage('lt_join_error') si joinWithCode fail pour debug + reconnexion. Rebuild + redeploy firestore.rules. Tests 244/244. |
| 113 | **Fix critique + dark mode Cautions + rename Frais->Lettre** | `src/App.tsx`, `src/pages/Caut.tsx`, `src/components/dossiers/NDosForm.tsx` | **Bug critique en prod** : `App.tsx:95 companyName={L.db.name}` — `L.db` n'etait plus expose par useAppLogic apres refactor E (split God-hook). Crash ErrorBoundary affichait "SAPURAI - Une erreur est survenue" pour les agents invites, bloquant aussi l'admin sur la meme machine. Fix : `(props.db || {}).name || ""` (pattern identique aux 4 autres usages du fichier). **Dark mode tabs Cautions** : fond actif utilisait `var(--btn-primary-bg)` qui s'inverse en dark (devient clair) mais texte hardcode "white" restait blanc => invisible en dark. Remplace par `var(--text-primary)` (fond) + `var(--bg-primary)` (texte) — ces 2 vars s'inversent ensemble => contraste garanti dans les 2 themes. Sous-texte "N a recuperer" : `#fecaca` hardcode remplace par `var(--danger-light)` theme-aware. Retrait des 2 eslint-disable devenus inutiles. **Rename Frais->Lettre** : bloc resume "Louees — Montants" et colonne tableau LOUEES passent de "Frais" a "Lettre" (plus precis : c'est la lettre de garantie qui est facturee). Label formulaire NDosForm idem : "Lettre (frais location) FCFA". Build + tests 244/244 verts. |
| 112 | **Tests F.2 : syncDPWorld + auto-stub integration** | `src/services/__tests__/dpworld.test.ts` (CREE), `src/hooks/__tests__/useDPWorldSync.test.ts` (CREE), `src/hooks/__tests__/autoStub.integration.test.ts` (CREE) | Filet de tests sur les fonctions critiques metier. **dpworld.test.ts** (18 tests) : `mapDPWorldToPatches` couvert — mapping da depuis ATA la plus ancienne, BAD/BAE avec dates, pregate, TC matching normalise (espaces/tirets ignores), TC ATTENDU=>PORT, TC PORT=>DISPATCHE si visitState=3DEPARTED, summary vide/3-premiers/...). **useDPWorldSync.test.ts** (10 tests) : flow complet avec `vi.mock` sur `fetchDPWorld`, verifie patches appliques, auto-stub declenche sur nouvelle `da`, anti-doublon, pas de stub si `da` deja present, dedup BL identique dans syncAllDPWorld, skip CLOTURE/ARCHIVE, erreur reseau ne plante pas. **autoStub.integration.test.ts** (8 tests) : integration via useAppLogic — patchDos(da) declenche stubs, editDos idem, TRANSIT+garantie permanente exclut caution, ignoreDep soft-delete empeche re-stub, toggleDepSt sur stub passe a PAYE. **Total tests** : 208 => 244 (+36). Build OK, lint 0 errors. shareTracking non teste en integration (depend de Firebase Auth/Firestore — extraction d'helper pur reportee au besoin). |
| 111 | **Refactor DetView en onglets (tabs)** | `src/components/dossiers/DetView.tsx` | Page detail dossier reorganisee en 4 onglets — UX mobile ameliore (~800 lignes de scroll vertical => sections focalisees). Hero + Phrase humaine + menu + Export PDF restent toujours visibles (wrapper exterieur). Contenu des tabs : **Vue generale** (Documents BAD/BAE/Pregate + Caution), **Conteneurs** (Franchise surestaries + liste TCs avec stepper), **Depenses** (liste depenses + Rentabilite Recette/Marge), **Documents & Historique** (Intervenants + Documents joints + Log). TabBar accessible (`role="tablist"`, `role="tab"`, `aria-selected`, `aria-controls`), style active = border-bottom + fontWeight 700, persistance via useState (pas URL hash pour v1). Build + tests 208/208 verts. |
| 110 | **Migration ClickableDiv : 40 div onClick migres** | `src/components/ui/ClickableDiv.tsx`, `src/pages/Dash.tsx`, `src/pages/Caut.tsx`, `src/pages/Dos.tsx`, `src/components/dossiers/DetView.tsx` | Accessibilite WCAG 2.1 AA : div cliquables avec support clavier (Enter/Space), role=button, tabindex, aria-label. **ClickableDiv enrichi** : supporte `stopPropagation?: boolean`, `title?: string`, type MouseEvent passe a onClick. **Migration** : Dash.tsx 5/5, Caut.tsx 5/5, DetView.tsx 13/13 (menu dropdown 9 items + cards BAD/BAE/Pregate 3 avec `disabled={!ce}`), Dos.tsx 13/19 (row desktop refactor : 9 cellules onClick individuelles => 1 ClickableDiv sur la row entiere, gain a11y majeur). **7 div onClick restants legitimes** : backdrops modales (AppModals, Overlay dialog, Dos.tsx context menu parent) + commentaires de doc dans ClickableDiv.tsx lui-meme. Build 16s, tests 208/208, lint 0 errors. |
| 109 | **Purge 25 hex warnings ESLint** | `src/Landing.tsx`, `src/pages/AdminPanel.tsx`, `src/components/layout/TopBar.tsx`, `src/ImportExcel.tsx`, `src/pages/Dash.tsx`, `src/pages/Dos.tsx`, `src/pages/Caut.tsx`, `src/components/dossiers/IntervenantsView.tsx`, `src/pages/Chs.tsx`, `src/pages/AgentView.tsx`, `src/components/ui/FormField.tsx`, `src/TrackingPage.tsx` | 25 hex hardcoded remplaces ou annotes avec justification. **Remplaces** : `#fef2f2/#dc2626/#fecaca` TopBar => var(--danger-bg/danger/danger-border) ; `#e0e7ff/#4338ca/#c7d2fe` AdminPanel => var(--info-bg/info-text/info-border) ; `#7f1d1d` => var(--danger-text) ; `#374151` ImportExcel => var(--text-tertiary) ; `#d6d3d1` Chs/AgentView => var(--border) ; `#a8a29e` FormField => var(--text-muted) ; fallbacks `var(--x, #hex)` supprimes (var toujours definie). **Annotes** (exceptions legitimes) : 5 gradients dark permanents Landing/TrackingPage (s'invertiraient en dark mode), 2 WhatsApp brand green `#25d366` Dash/Dos, 2 colors permanentes sur fond dark Caut.tsx tabs actifs. 321 => 294 warnings (-27). Build 11.74s, tests 208/208. |
| 108 | **Auto-stub Depenses : commit 4 (UI visible — widget Dash, filtres Dep, soft-delete)** | `src/utils/pendingInvoices.ts` (CREE), `src/utils/__tests__/pendingInvoices.test.ts` (CREE), `src/pages/Dash.tsx`, `src/pages/Dep.tsx`, `src/hooks/useDepenseActions.ts`, `src/hooks/useAppLogic.ts`, `src/App.tsx` | UI bout-en-bout livree. **pendingInvoices.ts** : helper pur pour le widget Dash — `pendingInvoices(dep, dos)` filtre status=en_attente_facture + !ignored + dossier non archive, trie par age decroissant ; `pendingInvoicesStats()` calcule total/retards(>10j)/cette semaine/dossiers ; `groupPendingByDossier()` regroupe par dossier avec maxAge. Seuil retard `LATE_THRESHOLD_DAYS = 10j` (decision metier user). **Dash** : nouveau widget au-dessus de "Problemes signales" — bandeau couleur (warning si retards > 0, info sinon), stats en en-tete (total, retards, cette semaine, dossiers), liste top 5 dossiers cliquables (redirigent vers det) avec badge age+compteur factures. **Dep** : (1) nouveau tab "En attente" entre Toutes et A payer, filtre par `status === 'en_attente_facture'`, (2) exclusion systematique des `ignored` de toutes les vues (soft-delete), (3) badge `AUTO` sur les stubs auto a cote du type, (4) bouton statut specialise "En attente" (bleu info) pour les stubs — clic ouvre la modale edition pour saisir le montant, (5) nouvelle action "Ignorer" (emoji stop) visible uniquement sur stubs auto en attente. **useDepenseActions** : ajout `ignoreDep(id)` soft-delete + action log IGNORE_DEPENSE, expose via orchestrator + prop App.tsx. **Tests** : +7 sur pendingInvoices (filtre status/ignored/archive, age/late, tri, stats, groupByDossier, seuil 10j). 201 → 208 tests verts. Build 26.82s, lint 0 errors. **Chantier auto-stub complete** (commits 1+2+3+4 = 4 livraisons incrementales, ~49→208+49=76 nouveaux tests au total sur le chantier, 13 fichiers modifies, 5 crees). |
| 107 | **Auto-stub Depenses : commit 3 (declencheurs actifs)** | `src/utils/stub.ts`, `src/utils/__tests__/stub.test.ts`, `src/hooks/useDossierActions.ts`, `src/hooks/useDPWorldSync.ts`, `src/components/dossiers/NDepForm.tsx` | Activation bout-en-bout de l'auto-stub. **utils/stub.ts** : 2 helpers ajoutes — `isNewArrival(oldDos, newDos)` (true si `da` passe d'absent a present), `generateArrivalStubsWithIds(dos, dep, mkId, ctx)` (variante de stubDepensesArrivee qui filtre les Depenses du dossier courant, genere les ids, retourne Depense[] pret a push dans db.dep). **useDossierActions** : `patchDos` et `editDos` detectent la nouvelle arrivee et concatenent les stubs dans `db.dep` avant `sv()` — meme transaction, zero race. **useDPWorldSync** : `syncDPWorld` resout le patchedDos, si `isNewArrival(d, patchedDos)` alors stubs ajoutes + summary enrichi " +N factures en attente". `syncAllDPWorld` applique le meme pattern en boucle, accumule les stubs dans `accumulatedDep` pour eviter doublons inter-dossiers au sein du meme batch. **NDepForm** : 2 nouveaux selects dans la grille principale — `Catégorie` (11 valeurs du type DepenseCategorie, libelles FR) et `Statut` (en_attente_facture / a_payer / payee). onSave maintient sync `s` legacy (payee → PAYE, sinon ATT) + propage `auto` et `ignored` depuis init. **Tests** : +4 sur `stub.test.ts` (isNewArrival 3 cas + generateArrivalStubsWithIds filtre par did). 197 → 201 tests verts. Build 12.48s, lint 0 errors. **Effet visible en prod** : des la prochaine sync DPWorld ou saisie manuelle de `da` sur un dossier sans date d'arrivee, les factures attendues se creent automatiquement dans la page Depenses (non filtrees pour le moment — UI widget Dashboard arrive au commit 4). |
| 106 | **Auto-stub Depenses : commit 2 (formulaire dossier)** | `src/components/dossiers/NDosForm.tsx` | Ajout de 6 champs dans le formulaire creation/modification dossier. **UI** : (1) select `Type dossier` dans la grille principale (IMPORT / TRANSIT / VEHICULE) a cote de `Garantie` — reset auto de `besc` si non-IMPORT et de `ror` si non-VEHICULE. (2) Nouvelle section "Parametres metier (auto-stub factures)" avant le bloc TC, avec note UX "Laisser vide pour appliquer les valeurs par defaut". Grille 3 colonnes pour les 3 franchises (`frCp`, `frMg`, `frRt`) en input number avec placeholder dynamique affichant la valeur par defaut calculee (ex: "10 (IMPORT)", "4 (DAKAR)"). (3) Deux checkboxes en ligne : `BESC a prevoir` (toujours visible, defaut true) et `Vehicule sur navire (RoRo, pas de retour TC)` (visible uniquement si td === VEHICULE). **Save** : normalisation des franchises — champ vide => undefined (recalcul dynamique via getFranchise* cote consommateur), sinon parseInt. Type du payload passe a `Record<string, unknown>` pour clean sans `any`. **Regles Firestore** : aucun changement requis — les Dossier sont stockes dans le doc company (pas en sous-collection), la regle existante `allow update` admin/editor couvre automatiquement les nouveaux champs. **Verifications** : build 10.87s, lint 0 errors, tests 197/197 inchanges. |
| 105 | **Auto-stub Depenses : commit 1 (fondations types + utils + tests)** | `src/types.ts`, `src/utils/franchise.ts` (CREE), `src/utils/stub.ts` (CREE), `src/utils/__tests__/franchise.test.ts` (CREE), `src/utils/__tests__/stub.test.ts` (CREE) | Premier commit du chantier auto-stub Depenses a l'arrivee d'un TC (DPWorld sync ou saisie manuelle `da`). **Scope commit 1** : fondations pures, zero UI, zero declencheur actif. **types.ts** : 6 champs optionnels sur Dossier (`td` type IMPORT/TRANSIT/VEHICULE, `frCp`/`frMg`/`frRt` franchises, `besc` toggle, `ror` RoRo), 4 sur Depense (`status` en_attente_facture/a_payer/payee, `auto`, `ignored` soft-delete, `categorie`), nouveau type `DepenseCategorie` (11 valeurs). **utils/franchise.ts** : `regionFromDestination()` (heuristique Dakar/Thies/Corridor Mali+Burkina+Niger/Senegal), defaults `defaultFranchiseMagasinage` (IMPORT=10, TRANSIT=21, VEHICULE=5), `defaultFranchiseCompagnie` (10j), `defaultFranchiseRetourVide` (DAKAR=4, THIES=5, SENEGAL=8, CORRIDOR=23), getters avec priorite override. **utils/stub.ts** : catalogue 11 categories avec meta (tp/fournisseur/libelle), `guessCategorie` (devine categorie legacy depuis libelle+tp), `guessStatus` (PAYE→payee sinon a_payer), `categoriesToStubArrivee` (ordre metier : location, debarquement, caution+garantie si TRANSIT sans perm, surestaries si franchise depassee, besc si toggle, orbus, dpworld), `categoriesToStubRetourVide` (detention_vide si franchise depassee ET !ror), `filterAntiDoublon` (par champ categorie OU devinette sur legacy), `buildStubDepenses` (construit Omit<Depense,'id'>[] avec auto=true/status=en_attente_facture/mt=0/s=ATT compat). **Tests** : 17 franchise (regions, defaults, overrides), 32 stub (guessCategorie, categoriesToStubArrivee IMPORT/TRANSIT/VEHICULE, ordre metier, franchises, anti-doublon, RoRo skip, end-to-end). **Verifications** : 148 → 197 tests (+49), build OK (17.83s), lint 0 errors (warnings pre-existants non lies). Prochains commits (2-4) : formulaire dossier, declencheurs DPWorld/arriveeReelle, UI Dashboard + filtres Depenses. |
| 103 | **Doc : nettoyage branding LogiTrans → Sapurai (README, CLAUDE, .docx)** | `README.md` (REWRITE), `CLAUDE.md`, `LogiTrans_*.docx` (RENAME) | Suite de la tache 102. (1) README.md : titre "LogiTrans V3" → "Sapurai V4", references `.jsx`/`.js` → `.tsx`/`.ts`, structure fichiers a jour (pages/, hooks/, services/, components/ui/, utils elargi), commandes ajoutees (lint:fix, format, typecheck, test:watch, preview), section securite reformulee (les Firestore Rules existantes ne doivent pas etre remplacees par allow read,write:if true), mention Google Sign-In en plus d'Email/Password, Node.js 20+. (2) CLAUDE.md : titre + tableau "Fichiers cles" mis a jour avec les .ts, ajout des hooks specialises (useAppMetrics, useDossierActions), ajout types.ts et STYLE-GUIDE.md a la liste. (3) Renommage des 5 fichiers .docx : LogiTrans_Bilan_Etape, LogiTrans_Bilan_Parcours, LogiTrans_Bilan_Phase1, LogiTrans_Plan_Execution_Marketplace, LogiTrans_Sprint13_Plan → prefixe Sapurai_. Contenu interne des .docx **non modifie** (artefacts historiques, le contenu reste sur la denomination de l'epoque ; seul le nom de fichier est aligne sur le branding actuel). CV_Ousmane_Approvisionnement_v2.docx ignore (sans rapport avec le projet). Verifie : COMPETITIVE-BRIEF, EXECUTIVE-ONEPAGER, SALES-BATTLECARDS, WIN-VS-WEBGRAM ne contenaient deja plus de mention LogiTrans. |

---

## FAIT — Sprint 14 : Integration DPWorld + Corrections

| # | Tache | Fichiers modifies | Details |
|---|-------|-------------------|---------|
| 74 | Sync DPWorld 1-clic | `services/dpworld.ts` (CREE), `hooks/useAppLogic.ts`, `App.tsx`, `components/shared/AppModals.tsx`, `components/dossiers/DetView.tsx` | Bouton "Sync DPWorld" dans le menu dossier. Appelle l'API DPWorld via proxy Cloudflare Worker. Met a jour automatiquement : date arrivee (da), BAD/BAE statut, pregate numero, et statut TCs (ATTENDU→PORT, PORT→DISPATCHE). Regle : ne jamais ecraser un champ deja renseigne. Log SYNC_DPWORLD. |
| 75 | Sync All DPWorld | `hooks/useAppLogic.ts`, `App.tsx`, `pages/Dash.tsx` | Bouton "Sync DPWorld (tous)" dans le Dashboard. Synchro de tous les dossiers actifs ayant un BL en une seule action. Cache les BL deja appeles. Ecriture unique en fin de batch. |
| 76 | Fix date BAD DPWorld | `services/dpworld.ts` | Utilisation de validiteDODate (date validite DO) au lieu de dateBad (date emission). Permet la mise a jour si DPWorld fournit une date plus recente que celle existante. Corrige les fausses surestaries. |
| 77 | Urgences BAD/BAE au port | `hooks/useAppLogic.ts` | Alertes "TC au port, BAD non demande" et "TC au port, BAE non demande" quand un dossier a un TC au PORT mais BAD/BAE encore NON_DEMANDE. Philosophie : filet de securite, pas automate. |
| 78 | Fix React is not defined | `pages/Dos.tsx` + tous les imports `.tsx` | Cause : `React.Fragment` utilise sans import React dans Dos.tsx. Fix : remplace par `<>`. Aussi corrige tous les imports .jsx → .tsx. |
| 79 | Auto-sync DPWorld 60min | `App.tsx` | setInterval qui appelle syncAllDPWorld toutes les 60 min si l'app est visible. Pas de spam : verifie document.visibilityState. |
| 80 | Suppression carriers.ts | `services/carriers.ts` (SUPPRIME), `hooks/useAppLogic.ts` | Code mort : APIs Maersk/CMA/Hapag non utilisees (pas de cles). Remplace par DPWorld. 209 lignes supprimees. |

---

## FAIT — Sprint 13 : Marge visible, Toggle docs, WhatsApp relances

| # | Tache | Fichiers modifies | Details |
|---|-------|-------------------|---------|
| 65 | Colonne Marge dans liste dossiers | `pages/Dos.tsx` | Colonne "Marge" (rv - depenses) dans le tableau desktop avec tri, footer total marge, et bloc MARGE en vue cards. Vert si positive, rouge si negative, "---" si recette non renseignee. |
| 66 | KPI Marge dans Dashboard | `pages/Dash.tsx` | Carte KPI "Marge globale" (totalRv - totalDep) dans les summary cards. Colonne marge par client dans le suivi financier. |
| 67 | Marge dans exports Excel | `utils/export.ts` | Colonnes "Recette" et "Marge" ajoutees dans exportDossiers (feuille Dossiers) et exportFinancierClient (feuille Resume par client + ligne totaux). |
| 68 | Marge dans exports PDF | `utils/pdf.ts` | pdfDossier : section Recette/Marge apres les totaux (si rv > 0). pdfBilan : ligne "Financier" avec total/paye/impaye/marge. pdfClient : indicateurs Recette et Marge dans le resume client. |
| 69 | Toggle BAD cliquable | `components/dossiers/DetView.tsx`, `hooks/useAppLogic.ts` | Card BAD cliquable : cycle NON_DEMANDE → EN_COURS → OBTENU au clic. Date validite inline pre-remplie (optionnelle). Fonction patchDos() pour merge partiel + log. |
| 70 | Toggle BAE cliquable | `components/dossiers/DetView.tsx` | Card BAE cliquable : meme cycle. Saisie N° declaration inline quand OBTENU. |
| 71 | Toggle Pregate cliquable | `components/dossiers/DetView.tsx` | Card Pregate cliquable : input texte inline pour saisir/modifier/effacer le numero. |
| 72 | Urgence BAD sans date | `hooks/useAppLogic.ts` | Si BAD OBTENU sans date de validite (d.bv vide), urgence warning "BAD sans date validite" dans la categorie BAD. Rappel doux pour completer. |
| 73 | WhatsApp relances impayes | `pages/Dash.tsx` | Bouton WhatsApp vert par client dans le suivi financier (si impaye > 0 et tel client connu). Message pre-rempli : salutation, montant total du, detail BL par BL (max 10), signature entreprise. Ouvre wa.me dans nouvel onglet. |

## FAIT — Sprint 12 : Migration TypeScript complete

| # | Tache | Fichiers modifies | Details |
|---|-------|-------------------|---------|
| 64 | Migration TypeScript complete | 62 fichiers `.js/.jsx` → `.ts/.tsx`, `index.html` | **Renommage** : 4 constants, 8 utils, 3 tests, 4 hooks, 1 service, 6 UI, 2 layout, 8 shared, 9 feature, 9 pages, 8 root → tous en `.ts/.tsx`. **Types ajoutes** : interfaces Props sur tous les composants React, types parametres/retours sur hooks/utils/services, `CSSProperties` pour styles, generiques sur `usePagination<T>`, interfaces domaine (`CarrierConfig`, `TarifConfig`, `UseAppLogicParams`). `sw.js` reste en JS (service worker non bundle). 0 changement fonctionnel. Build 0 erreurs, 108 tests, 0 erreurs lint (181 warnings `no-explicit-any`). |

## FAIT — Sprint 11 : PDF client, stats transport, mode hors-ligne

| # | Tache | Fichiers modifies | Details |
|---|-------|-------------------|---------|
| 61 | PDF bilan par client | `utils/pdf.js`, `pages/Dash.jsx` | Nouvelle fonction `pdfClient()` : resume client (nb dossiers, total/paye/impaye, taux), recapitulatif dossiers, puis detail par dossier (conteneurs + depenses + sous-totaux). Bouton PDF par client dans le suivi financier dashboard. |
| 62 | Stats transport (chauffeur, type TC, poids) | `pages/Stats.jsx` | Nouvelle section dans Statistiques : transport par chauffeur (nb voyages, prix moyen), prix moyen par type TC (20GP/40GP/40HC), poids moyen par type TC. Croise dep TRANSPORT avec tcs via tcid. |
| 63 | Amelioration mode hors-ligne | `sw.js`, `TopBar.jsx` | SW v4 : cache-first pour assets statiques (JS/CSS/fonts), network-first pour pages HTML, fallback index.html. Indicateur "Hors ligne" rouge dans la TopBar quand pas de connexion. Firestore offline deja actif (persistentLocalCache). |

## FAIT — Sprint 10 : Dashboard financier, filtre client, tests

| # | Tache | Fichiers modifies | Details |
|---|-------|-------------------|---------|
| 57 | Extraction helpers import → utils | `utils/importHelpers.js` (nouveau), `ImportExcel.jsx` | 16 fonctions pures + 3 constantes extraites de ImportExcel.jsx vers un module testable. ImportExcel importe depuis le module. Zero changement fonctionnel. |
| 58 | Tests unitaires import helpers | `utils/__tests__/importHelpers.test.js` (nouveau) | 80 tests couvrant splitContainers, normBL, normType, isBLNumber, isContainerNumber, isAmount, isDate, isClientName, isTCType, parseDate, parseNum, detectSheetType, findCol. Total : 108 tests (28 existants + 80 nouveaux). |
| 59 | Filtre client dashboard → dossiers | `App.jsx`, `Dos.jsx`, `Dash.jsx` | Etat `filtCl` leve dans App.jsx, passe a Dash (setFilter) et Dos (filtCl/setFiltCl). Clic sur un client dans le suivi financier ouvre la page dossiers filtree sur ce client. Reset auto quand on quitte la vue dossiers. |
| 60 | Export financier par client (Excel) | `utils/export.js`, `Dash.jsx` | Nouvelle fonction `exportFinancierClient()` : feuille 1 resume par client (nb dossiers, total, paye, impaye, % + ligne totaux), feuille 2 detail factures groupees par client. Bouton "↓ Excel" dans le header du suivi financier dashboard. |

## FAIT — Sprint 9 : Import robuste + API Carriers

| # | Tache | Fichiers modifies | Details |
|---|-------|-------------------|---------|
| 51 | Multi-TC par cellule a l'import | `ImportExcel.jsx` | Nouvelle fonction `splitContainers()` qui decoupe les cellules contenant plusieurs numeros TC separes par `/`, `,`, `;` ou retour a la ligne. Gere aussi les espaces internes (`HLBU 3170520`). Applique dans 3 endroits : creation dossier, ajout TC supplementaire, feuille conteneurs separee, et pattern scanner. Detection colonnes elargie au pluriel (`conteneurs`, `numconteneurs`). |
| 52 | Normalisation BL numeriques | `ImportExcel.jsx` | Fonction `normBL()` qui supprime le `.0` des BL numeriques Excel (ex: `259330400.0` → `259330400`). Applique aussi dans `isBLNumber()` du pattern scanner. |
| 53 | Type TC apostrophes/guillemets | `ImportExcel.jsx` | `normType()` gere maintenant `20'`, `40''`, `40 PIEDS` en plus des formats standard. Nettoyage apostrophes, guillemets et "PIEDS" avant detection. |
| 54 | Depenses en colonnes (pivot) | `ImportExcel.jsx` | Section 3b : detection automatique des feuilles avec colonnes d'expenses (Location, Frais add, Surestaries, Pregate, etc.). Chaque colonne non-zero genere 1 ligne de depense avec le type correct. Enrichit aussi BAD/BAE des dossiers. Detection type feuille elargie (keywords: surestari, pregate, location, situation). |
| 55 | API compagnies maritimes — ETA auto | `services/carriers.js` (nouveau), `SettingsForm.jsx`, `useAppLogic.js` | Connecteurs API pour MAERSK, CMA CGM, HAPAG-LLOYD, MSC (DCSA Track & Trace). Detection automatique du carrier via prefixe BL/TC. Auto-refresh ETA au demarrage avec cooldown 6h (localStorage). Cles API configurables dans Parametres. Batching 10 requetes max simultanées. Mise a jour `d.da` + marqueur `etaSrc: "API"`. |
| 56 | Tableau financier par client (Dashboard) | `Dash.jsx` | Remplacement de "Reprendre le fil" par un tableau recapitulatif par client : nombre de dossiers, total depenses, paye, impaye, barre de progression. Trie par impaye decroissant (plus gros debiteurs en premier). Couleurs vert/orange/rouge selon % paiement. Nettoyage imports inutilises (tcSum, DBG, DC, DL, recent3). |

## FAIT — Sprint 8 : Qualite, filtres, badges

| # | Tache | Fichiers modifies | Details |
|---|-------|-------------------|---------|
| 45 | Deduplication import conteneurs | `useAppLogic.js` | Verification unicite numero TC avant ajout dans addDos. Les doublons sont ignores avec notification warning. |
| 46 | Surestaries dashboard (jours) | `Dash.jsx` | KPI surestaries affiche le nombre de jours cumules au lieu du montant FCFA (depend de chaque compagnie). |
| 47 | Filtre par client | `Tcs.jsx` | Ajout champ "Filtrer par client" a cote du filtre chauffeur. Filtrage sur `d.cl` du dossier lie au TC. |
| 48 | Cleanup ESLint warnings | ~30 fichiers | Suppression imports React inutiles (10 standalone + 18 destructured), unused `useState` BilanView, unused `useEffect` Setup, unused `useMemo`/`usePagination` Dos. 82→45 warnings. |
| 49 | Badge "Arrive J-X" pour ATTENDU | `useAppLogic.js` | `tcFranchise` retourne un badge "Arrive J-X" pour les TC en ATTENDU avec countdown vers date d'arrivee. Couleurs vert/orange/rouge selon proximite. |
| 50 | Validation poids import Excel | `ImportExcel.jsx` | Indicateur "X sans poids" en rouge dans la barre de stats avant import. Alerte visuelle pour les TC avec poids 0. |

## FAIT — Sprint 7 : Cycle de vie ATTENDU + Caution dossier

| # | Tache | Fichiers modifies | Details |
|---|-------|-------------------|---------|
| 41 | Statut ATTENDU dans le cycle TC | `statuts.js`, `theme.css`, `useAppLogic.js`, `date.ts`, `Tcs.jsx`, `Dos.jsx`, `Dash.jsx`, `Chs.jsx`, `App.jsx`, `DispForm.jsx`, `BilanView.jsx`, `pdf.js`, `DetView.jsx`, `types.ts` | Nouveau statut ATTENDU en tete du cycle (ATTENDU→PORT→...→RETURNED). Auto-promotion via useEffect quand `d.da <= today`. Bouton manuel "Arrive au port". Couleur slate (#64748b/#94a3b8). Compteur nAttendu, exclusion franchise/urgences. ~14 fichiers. |
| 43 | Fix useMemo → useEffect (auto-promotion) | `useAppLogic.js` | Anti-pattern corrige : `useMemo` avec side-effect (`sv()`) remplace par `useEffect`. Dependances elargies a `[tcs, dos, todayStr]` pour reagir aux updates Firestore multi-utilisateur. Garde `toPromote.length > 0` empeche la boucle. |
| 44 | Sync bidirectionnelle ATTENDU ↔ PORT | `useAppLogic.js` | Ajout retrogradation PORT → ATTENDU quand `d.da > today`. Les conteneurs existants avec date d'arrivee future repassent en ATTENDU. Pas de risque de boucle (apres sv, conditions ne matchent plus). |
| 42 | Caution au niveau dossier (refactoring) | `NDosForm.jsx`, `useAppLogic.js`, `DispForm.jsx`, `DetView.jsx`, `Caut.jsx`, `App.jsx`, `types.ts` | Deplacement garantie de `tc.garantie` vers `d.gar_*` (gar_contact, gar_tel, gar_frais, gar_caution, gar_statut). NDosForm : champs conditionnels LOUEE/VENDUE. DispForm : section garantie supprimee. DetView : affichage caution dossier + boutons action. Caut.jsx : refonte avec totaux financiers, KPI cautions en cours, boutons recuperer/rembourser. useAppLogic : urgences et compteurs sur dossier. Interface Garantie supprimee de types.ts, champs gar_* ajoutes a Dossier. |

## FAIT — Cosmétique / Branding

| # | Tache | Fichiers modifies | Details |
|---|-------|-------------------|---------|
| 39 | Favicon SVG (camion) | `public/favicon.svg`, `index.html` | Icone camion minimaliste stone-800 dans l'onglet navigateur. `<link rel="icon">` ajoute. |
| 40 | Accents francais dans l'UI | `Sidebar.jsx`, `TopBar.jsx`, `Dep.jsx`, `Dos.jsx`, `Dash.jsx`, `AppModals.jsx`, `SettingsForm.jsx`, `NDosForm.jsx`, `NDepForm.jsx`, `DetView.jsx`, `App.jsx` | ~25 corrections : Depenses→Dépenses, Parametres→Paramètres, Clotures→Clôturés, Deconnexion→Déconnexion, entree→entrée, resultat→résultat, etc. |

## FAIT — Securite

| # | Tache | Fichiers modifies | Details |
|---|-------|-------------------|---------|
| 1 | Firestore rules `/invites` restreintes | `firestore.rules` | Separation read/create/delete avec validation des champs requis (companyId, createdBy) |
| 2 | Super-admin UID externalise en env var | `main.jsx`, `.env` | `VITE_SUPER_ADMIN_UID` au lieu du UID hardcode |
| 3 | Messages d'erreur login generalises | `Login.jsx` | "Email ou mot de passe incorrect" au lieu de messages specifiques (anti-enumeration) |
| 4 | Cle API Gemini dans header au lieu de l'URL | `ScanBL.jsx` | `x-goog-api-key` header au lieu de `?key=` en query param |
| 5 | Headers de securite Firebase Hosting | `firebase.json` | X-Frame-Options, X-Content-Type-Options, Referrer-Policy, Permissions-Policy |

## FAIT — UX / Accessibilite

| # | Tache | Fichiers modifies | Details |
|---|-------|-------------------|---------|
| 6 | Debounce recherche globale (250ms) | `TopBar.jsx` | localGs + debounceRef pour eviter les re-renders a chaque frappe |
| 7 | Fermeture modale par Escape + focus trap | `Overlay.jsx` | useEffect keydown + auto-focus premier element interactif |
| 8 | HTML semantique (nav, main) | `Sidebar.jsx`, `App.jsx` | `<nav>` au lieu de `<div>`, `<main>` au lieu de `<div>` |
| 9 | Attributs ARIA | `Sidebar.jsx`, `TopBar.jsx`, `Overlay.jsx`, `App.jsx` | role, aria-label, aria-modal, aria-current, aria-expanded, aria-hidden |
| 10 | Toast accessible | `App.jsx` | `role="alert" aria-live="polite"` |

## FAIT — Performance

| # | Tache | Fichiers modifies | Details |
|---|-------|-------------------|---------|
| 11 | useMemo sur counters et urgences | `useAppLogic.js` | Memoisation des calculs couteux (deps: dos, tcs, dep, cfg) |

## FAIT — Qualite de code

| # | Tache | Fichiers crees/modifies | Details |
|---|-------|-------------------------|---------|
| 12 | ESLint + Prettier | `.prettierrc`, `eslint.config.js`, `package.json` | 0 erreurs, 76 warnings (no-unused-vars, no-console) |
| 13 | Fix hooks conditionnels | `DetView.jsx`, `IntervenantsView.jsx`, `JdocView.jsx`, `PregateInput.jsx` | useState deplace avant les early returns (15 erreurs corrigees) |
| 14 | Composants UI reutilisables | `src/components/ui/Btn.jsx`, `Badge.jsx`, `Card.jsx`, `FormField.jsx` | Remplacement des boutons inline dans NDosForm, NDepForm, NChForm |
| 15 | Pagination des listes | `src/hooks/usePagination.js`, `src/components/ui/Pagination.jsx`, `Dos.jsx`, `Dep.jsx`, `Chs.jsx`, `Tcs.jsx`, `Caut.jsx` | 20 items/page (Dos, Dep, Tcs, Caut louees/vendues), 30/page (Chs), reset auto au changement de filtre |
| 16 | Tests unitaires Vitest | `src/utils/__tests__/date.test.js`, `format.test.js` | 28 tests sur today, joursDiff, calcAlertesFranchise, calcUrgencesDoc, fm, tcSum, fd |

## FAIT — Admin Panel

| # | Tache | Fichiers modifies | Details |
|---|-------|-------------------|---------|
| 17 | Affichage des membres par entreprise | `AdminPanel.jsx` | Bouton "Membres" extensible, chargement lazy depuis `/companies/{id}/members`, affiche nom, email, role (admin/agent/membre), date d'inscription |
| 18 | Apercu enrichi des entreprises | `AdminPanel.jsx` | Ajout nb chauffeurs, nb depenses, derniere activite (date + action) dans chaque carte |
| 19 | Firestore rules super-admin | `firestore.rules` | Fonction `isSuperAdmin()` avec acces lecture/ecriture sur companies, members, notifications, config. Regle `/config/{docId}` ajoutee pour cle Gemini. Deploye sur Firebase. |

## FAIT — Sprint 1 : Acquisition + Data

| # | Tache | Fichiers modifies | Details |
|---|-------|-------------------|---------|
| 20 | Branding footer TrackingPage | `src/TrackingPage.jsx` | Lien cliquable "Suivi via **LogiTrans** — Essayer gratuitement" vers l'app, couleur #059669, dans les 2 footers (vue multi-dossier + vue unique) |
| 21 | Bouton WhatsApp visible dans Dos.jsx | `src/pages/Dos.jsx` | Bouton vert compact (📱) dans la carte mobile, visible si `d.ct` existe, meme message que le context menu existant |
| 22 | Champ "prix convenu" dans DispForm | `src/components/conteneurs/DispForm.jsx`, `src/hooks/useAppLogic.js` | Grille 3 colonnes (Budget/Avance/Prix convenu), stocke `tc.pc` via dispatch() |
| 23 | Montant surestaries FCFA sur Dashboard | `src/components/shared/SettingsForm.jsx`, `src/utils/date.js`, `src/hooks/useAppLogic.js`, `src/App.jsx`, `src/pages/Dash.jsx` | Tarif configurable (cfg.ts, defaut 25000), calcul jours depasses dans calcAlertesFranchise, card orange sur le dashboard |

## FAIT — Sprint 2 : UX + Garanties

| # | Tache | Fichiers modifies | Details |
|---|-------|-------------------|---------|
| 24 | Garanties integrees au dispatch | `src/components/conteneurs/DispForm.jsx`, `src/hooks/useAppLogic.js` | Section Garantie (PROPRE/LOUEE/PRETEE) avec champs conditionnels (contact, tel, frais, caution), objet compatible DetView, stocke `tc.garantie` |
| 25 | Simplifier Intervenants (notes) | `src/components/dossiers/IntervenantsView.jsx` | Textarea notes au lieu de checkboxes taches, retrocompat : anciennes taches affichees en badges read-only |

> Sprint 2 — taches non actives :
> - **Stats** : RETIREE (page deja correcte)
> - **Historique missions chauffeur** : DEJA FAIT (Chs.jsx lignes 288-307)

## FAIT — Sprint 4 : Finitions

| # | Tache | Fichiers modifies | Details |
|---|-------|-------------------|---------|
| 26 | Audit mobile + corrections Stats.jsx | `src/pages/Stats.jsx`, `src/styles/layout.css` | Boutons periode avec flexWrap, bar chart labels 70px + ellipsis, lt-grid4 → 1fr sur 480px |
| 27 | Reactivation page Cautions | `src/App.jsx`, `src/components/layout/Sidebar.jsx`, `src/pages/Dash.jsx` | Import lazy Caut.jsx, route `#caut` dans validVws, nav sidebar 🔒, card Dashboard cliquable "Voir →" |
| 28 | Pagination Tcs + Caut | `src/pages/Tcs.jsx`, `src/pages/Caut.jsx` | 20 items/page sur conteneurs et cautions (louees + vendues separement) |
| 29 | Carte PREGATE contextuelle | `src/components/dossiers/DetView.jsx` | Titre toujours "PREGATE", fond vert/orange/gris selon presence TC au port + numero renseigne |
| 30 | Tri intelligent par priorite | `src/pages/Dos.jsx`, `src/pages/Tcs.jsx` | Dossiers : BAD expire > surestaries > docs manquants > TC au port > transit. Conteneurs : franchise depasse > critique > OK > retourne. Departage par date arrivee |
| 31 | Fix tri : arrivees futures + date arrivee TC | `src/pages/Dos.jsx`, `src/pages/Tcs.jsx` | Dossiers avec date future classes apres arrives et en transit. Colonne "Arrivee" ajoutee dans la liste des conteneurs (desktop + mobile) |
| 32 | Skeleton screens (chargement) | `src/components/ui/Skeleton.jsx`, `src/styles/layout.css`, `src/main.jsx` | Composants Skeleton, RowSkeleton, TableSkeleton, CardSkeleton, PageSkeleton. Animation pulse CSS. PageSkeleton remplace le spinner au chargement initial. |
| 33 | Validation d'input formulaires | `src/utils/validate.js`, `NDosForm.jsx`, `DispForm.jsx`, `NDepForm.jsx`, `NChForm.jsx`, `SettingsForm.jsx`, `IntervenantsView.jsx` | Fonctions validate/validateAll + composant FieldError. maxLength HTML sur tous les inputs texte. Validation au submit : longueur max, bornes numeriques, format email. Messages d'erreur inline en rouge. |
| 34 | Tri configurable colonnes | `src/pages/Dos.jsx`, `src/pages/Tcs.jsx` | En-tetes desktop cliquables avec indicateur tri (fleche). Dossiers : Client, BL, Compagnie, Arrivee, Depenses, Statut. Conteneurs : Conteneur, Client, Arrivee, Poids, Statut. Tri priorite par defaut, bandeau info + bouton retour. Reset auto au changement de filtre. |
| 35 | Isolation fichiers par entreprise | `NDepForm.jsx`, `JdocView.jsx`, `AppModals.jsx`, `firestore.rules` | Cle fichier prefixee companyId (`lt-file-{companyId}-{uuid}`). Rules Firestore : create/update restreint a la propre entreprise, read/delete ouvert (retrocompat anciens fichiers). companyId passe aux composants NDepForm et JdocView. |

## FAIT — Sprint 5 : PDF + TypeScript

| # | Tache | Fichiers crees/modifies | Details |
|---|-------|-------------------------|---------|
| 36 | Rapports PDF (jsPDF + autoTable) | `src/utils/pdf.js` (cree), `DetView.jsx`, `Dep.jsx`, `Dash.jsx`, `App.jsx`, `vite.config.js`, `package.json` | 3 exports : pdfDossier (fiche complete A4 portrait), pdfDepenses (liste filtree A4 paysage), pdfBilan (situation + urgences + alertes). Chunk Rollup dedie (417 KB / 136 KB gzip). Bouton dans menu dossier, page depenses, et dashboard. |
| 37 | TypeScript setup + migration utils | `tsconfig.json` (cree), `src/types.ts` (cree), `eslint.config.js`, `package.json`, `src/utils/id.ts`, `format.ts`, `date.ts`, `validate.ts` | Config TS progressive (allowJs, strict:false, noEmit). Interfaces domaine (Dossier, Conteneur, Depense, Config, Alerte, Urgence, etc.). Bloc ESLint TS avec @typescript-eslint. Script `npm run typecheck`. 4 fichiers utils migres .js→.ts avec types. 28 tests passent. |

## FAIT — Sprint 6 : Dark Mode

| # | Tache | Fichiers crees/modifies | Details |
|---|-------|-------------------------|---------|
| 38 | Dark mode complet (CSS variables) | `src/styles/theme.css` (cree), `src/hooks/useTheme.js` (cree), `index.html`, `src/main.jsx`, `src/App.jsx`, `src/constants/styles.js`, `src/constants/statuts.js`, 5 UI components, `src/styles/layout.css`, `src/components/layout/Sidebar.jsx`, `src/components/layout/TopBar.jsx`, `src/components/shared/Overlay.jsx`, 10 pages, 14 feature components, `src/Setup.jsx`, `src/TrackingPage.jsx`, `src/ImportExcel.jsx`, `src/components/shared/BilanView.jsx` | ~50 fichiers modifies. Palette complete light/dark (Stone neutrals + semantiques). CSS variables dans `:root` et `[data-theme="dark"]`. Script anti-flash dans index.html. Hook useTheme (localStorage + prefers-color-scheme). Toggle lune/soleil dans Sidebar. Toutes les couleurs inline migrees vers var(). Couleurs PDF/print et data-viz conservees hardcoded. 0 erreurs build/lint, 28/28 tests. |

## Scripts disponibles

```bash
npm run dev          # Serveur de dev
npm run build        # Build production
npm run lint         # ESLint (0 erreurs attendues)
npm run lint:fix     # ESLint auto-fix
npm run format       # Prettier sur tout src/
npm test             # Vitest run (108 tests)
npm run test:watch   # Vitest watch mode
npm run typecheck    # TypeScript (0 erreurs sur fichiers .ts)
npm run deploy       # Build + deploy Firebase
```

---

## PAS FAIT — A traiter plus tard

### Priorite haute

| # | Tache | Raison du report | Effort estime |
|---|-------|------------------|---------------|
| A | **Migration sous-collections Firestore** | Refactoring majeur de useData.js + migration donnees existantes. Risque de regression eleve. | 2-3 jours |
| ~~B~~ | ~~**Isolation fichiers par entreprise**~~ | **FAIT** — Tache #35 | ~~1 jour~~ |
| C | **Proxy Gemini via Cloud Function** | Necessite plan Blaze (Cloud Functions). Cle API reste cote client. | 0.5 jour |
| D | **Firebase Custom Claims pour super-admin** | Necessite plan Blaze (Cloud Functions). Actuellement env var. | 0.5 jour |

### Priorite moyenne

| # | Tache | Raison du report | Effort estime |
|---|-------|------------------|---------------|
| ~~E~~ | ~~**Migration TypeScript**~~ | **FAIT** — Tache #37 (setup) + Tache #64 (migration complete 62 fichiers) | ~~Continu~~ |
| ~~F~~ | ~~**Skeleton screens**~~ | **FAIT** — Tache #32 | ~~0.5 jour~~ |
| ~~G~~ | ~~**Validation d'input**~~ | **FAIT** — Tache #33 | ~~1 jour~~ |
| H | **Rate limiting** | Necessite plan Blaze (Cloud Functions) ou Firebase App Check | 0.5 jour |

### Priorite basse

| # | Tache | Notes |
|---|-------|-------|
| ~~I~~ | ~~Dark mode~~ | **FAIT** — Tache #38 |
| ~~J~~ | ~~Tri configurable sur les listes~~ | **FAIT** — Tache #34 |
| ~~K~~ | ~~Rapports PDF~~ | **FAIT** — Tache #36 |
| L | Notifications push | Necessite Firebase Cloud Messaging + Cloud Functions |

---

## Architecture actuelle (rappel)

```
React 18.2 + Vite 7.3.1 + TypeScript (complet, 66 fichiers .ts/.tsx) + Firebase (Auth + Firestore)
1 document Firestore par entreprise (limite ~300 dossiers)
Deploye sur Firebase Hosting en PWA
PDF : jsPDF + jspdf-autotable (chunk dedie)
```

## Fichiers cles

| Fichier | Role |
|---------|------|
| `src/useData.js` | Hook central Firestore (listeners, CRUD, invites) |
| `src/hooks/useAppLogic.js` | Logique metier (counters, urgences, alertes) |
| `src/main.jsx` | Entry point, routing, auth guard |
| `src/App.jsx` | Shell principal (sidebar + pages) |
| `src/firebase.js` | Config Firebase |
| `firestore.rules` | Regles de securite Firestore |
| `firebase.json` | Config hosting + headers |

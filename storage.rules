rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Fichiers justificatifs — lecture/écriture pour les utilisateurs authentifiés uniquement
    match /files/{fileId} {
      allow read, write: if request.auth != null;
    }
  }
}
